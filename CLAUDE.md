# CLAUDE.md — הנחיות עבודה על קופת הנהג

פרויקט זה הוא אפליקציית ווב עברית (PWA) לניהול כסף של נהגי "דריבר" בישראל.
קלוד, עבוד על זה לפי ההנחיות הבאות.

## מי המדריך / בעל האפליקציה

- נהג דריבר ישראלי, עובד מהטלפון, רוצה פשטות וניקיון.
- קורא בעברית. כתוב תשובות והסברים בעברית, בסגנון ישיר, חם, קולע — בלי קישקושים.
- מעצב סגנון: אפליקציית פינטק מקצועית — **נקי, דק, חלק, יוקרתי, מינימלי. ללא צבעים מרובים, ללא אלמנטים ילדותיים**.

## המודל העסקי (חשוב! אל תשבור אותו)

- **סדרן**: מסדר נסיעות ומקבל **אחוז מכל נסיעה — לא בהכרח קבוע!** לנהגים שונים (ולפעמים לאותו נהג, נסיעה-נסיעה) יכול להיות אחוז שונה (למשל 10% מתחנה אחת, 30% מתחנה אחרת) — תלוי בחוזה מול התחנה/סדרן.
  - `settings.pct` הוא רק **ברירת המחדל** (מוצג בהגדרות). כל תנועת `rides` בפועל יכולה לשאת `tx.pct` משלה שדורס את ברירת המחדל.
  - `ridePct(t)` — הפונקציה היחידה שקובעת איזה % חל על תנועה: `t.pct!=null?t.pct:state.settings.pct`. **תמיד להשתמש בה**, לא ב-`state.settings.pct` ישירות, בכל מקום שמחשב עמלה/נטו לתנועת rides ספציפית.
  - `sums(list)` כבר מצרפת נכון per-transaction (`due` = סכום עמלות לפי ה-% של כל נסיעה בנפרד) — אל תחזרי לחישוב `rides_total * pct_גלובלי`.
  - UI: `#pctRow`/`#pctChips` במסך ההוספה (רק כש-`addType==='income' && addCat==='rides'`) — צ'יפים 10/20/25/30% + "אחר" (openSheet). הבחירה האחרונה נשמרת ב-`settings.lastRidePct` ומוצעת כברירת מחדל בפעם הבאה.
  - דוגמה: נסיעה אשדוד–בני ברק ₪180 בעמלה 30% → סדרן ₪54, נהג ₪126. אותה נסיעה בעמלה 10% → סדרן ₪18, נהג ₪162.
- **רכב**: 4 מצבי בעלות אפשריים ב-`settings.ownershipType`, נבחרים באשף הגדרה:
  - `rent` (שכירות) — תשלום חודשי קבוע, ביטוח בנפרד.
  - `finance` (מימון/הלוואה) — **שונה מליסינג!** החזר חודשי על הרכב + ביטוח משולם בנפרד לגמרי (שני שדות, שני סכומים).
  - `leasing` (ליסינג) — **שונה ממימון!** תשלום חודשי אחד שכולל הכל (ביטוח, טיפולים וכו') חוץ מדלק. באשף מדלגים אוטומטית על שלב הביטוח (`insuranceType='included'`, `insuranceMonthly=0`), ובהגדרות שורות הביטוח מוסתרות (`#insRow`,`#autoInsRow` עם `display:none`).
  - `owned` (בבעלות) — אין תשלום חודשי על הרכב עצמו, אבל עדיין יש ביטוח בנפרד.
  - שדה `settings.rent` הוא **הסכום החודשי הכולל** של שכירות/מימון/ליסינג (תלוי סוג) — ברירת מחדל **0**, לעולם לא ערך מומצא.
- `settings.billingDay` (1-31, ברירת מחדל 1) — יום החיוב בפועל בחודש (רלוונטי רק ל-rent/finance/leasing, לא ל-owned). נבחר באשף (`wizStep2b`) ובהגדרות (`data-act="set-billingday"`). `ensureRentExpense()` משתמשת בו לתאריך ההוצאה האוטומטית במקום להניח תמיד "1 לחודש" (`Math.min(billingDay, daysInMonth(y,m))` כדי לא לחרוג מסוף חודשים קצרים). שינוי הערך משפיע רק על חודשים עתידיים — לא נוגע בתנועות היסטוריות קיימות.
- **ביטוח**: `settings.insuranceMonthly` + `settings.insuranceType` (חובה/צד ג׳/מקיף/included). רלוונטי לכל הסוגים חוץ מ-leasing.
  - המספרים משתנים בין נהגים — אסור לתפור ערכים קבועים; חייב להיות ניתן לשינוי בהגדרות ובאשף.
  - `carCostBasisLbl()` — מחזיר תווית תצוגה מתאימה ("רכב+ביטוח" או "ליסינג, כולל הכל") לפי הסוג.
- רווח נטו = ברוטו − עמלת סדרן − הוצאות − עלות רכב יומית (`carDailyCost()` = (rent+insuranceMonthly)/workDays).
- עמלת סדרן מחושבת **רק על קטגוריית "נסיעות" (rides)**, לא על הכנסות אחרות.
- **נסיעה ששולמה ישירות לסדרן** (`ui.direct`, מתג `#directRow`/`#directSwitch` שמופיע רק כש-`isRideCat`): הלקוח שילם לסדרן ישירות, הנהג לא רואה מזומן. ב-`saveTx`: נרשמת תנועת ההכנסה הרגילה עם `tx.method='direct'`, ובמקביל **אוטומטית** נוצרת תנועת הוצאה מקושרת `{cat:'commission', amount: מלוא סכום הנסיעה}` (לא רק העמלה!) עם `linkedTx` שמצביע לנסיעה. זה בכוונה — כך `sums()` הקיימת (`com+=amount` לכל תנועת commission) כבר "מכסה" גם את העמלה של הנסיעה הזו וגם משאירה יתרה שמפחיתה חוב ישן, בלי צורך בשדות חדשים בלוגיקת הסיכום. **אל תשנה את זה ל"רק אחוז העמלה"** — המשתמש אישר במפורש שהסכום המלא יורד מהחוב.
- `METHODS.direct`='ישיר לסדרן' — נכנס אוטומטית לפירוט אמצעי תשלום בדוחות ולתגית ברשימת התנועות (קוד גנרי קיים, לא נדרש טיפול מיוחד).
- **תותים** (`cat:'totim'`, קטגוריית הוצאה, אייקון 🍓) — ניכוי קטן ולא קבוע שהסדרן מוריד מדי פעם. **זה מינוס, לא בונוס!** נספר בדיוק כמו כל הוצאה אחרת ב-`sums()` (`exp+=amount`) — אין צורך בלוגיקה מיוחדת.
- `settings.dayStartHour` (0-23, ברירת מחדל 0) — שעת תחילת "יום" עבור המשתמש (למשל 6 = יום מתחיל ב-6 בבוקר, לא בחצות). `todayISO()` היא **המקור היחיד** לחישוב "היום" בכל האפליקציה ומתחשבת בזה — אל תחזרי לחשב `new Date()` ישירות במקומות שאמורים לשקף את "היום הלוגי" של המשתמש.
- **"מה חדש"** (`APP_VER`, `WHATS_NEW`, `maybeShowWhatsNew()`): פופאפ שמוצג פעם אחת לכל גרסה, רק למשתמשים ש-`wizardDone` (לא למשתמש חדש שרק נרשם — אצלו `lastSeenVer` נקבע ל-`APP_VER` מיד ב-`cgMode==='new'`). **בכל שינוי משמעותי: הגדל את `APP_VER` ב-1 ועדכן את מערך `WHATS_NEW`** (2-4 שורות קצרות וברורות, לא טכניות) — אחרת המשתמשים לא יראו שהיה עדכון.

## מבנה קבצים

| קובץ | תפקיד |
|------|-------|
| `index.html` | האפליקציה כולה (HTML/CSS/JS מוטבע). גרסה 2.2 |
| `manifest.json` | מניפסט PWA |
| `sw.js` | Service Worker (אופליין + עדכונים) |
| `guide.html` | מדריך התקנה וענן בעברית |
| `.github/workflows/deploy.yml` | דיפלוי ל-Pages + יצירת `bc-config.js` מ-secret |
| `bc-config.js` | **לא בגיט** — allowlist של פיצ'ר ההפצה, נוצר רק ב-CI |
| `icon-192/512/180/maskable.png` | אייקונים |

## אחסון וסנכרון ענן (קוד אישי)

- **לוקלי**: `localStorage` מפתח `kupaNehed.v2` — תמיד מקור האמת המיידי במכשיר.
- **ענן**: Firebase Firestore, פרויקט קיים ומשותף עם אפליקציות אחרות של המשתמש (`sodg-272b7`) — **collection ייעודי ונפרד**: `kupaDriverAccounts`. אסור לגעת/לקרוא collections אחרים באותו פרויקט.
- **מנגנון**: כל משתמש בוחר "קוד אישי" חופשי (`settings.syncCode`, מינ' 4 תווים). מסמך Firestore: `kupaDriverAccounts/{code}` עם שדה `data` = כל ה-state כ-JSON string, ו-`updatedAt` (serverTimestamp, מטא בלבד — ההשוואה האמיתית היא על `state.updatedAt` הפנימי).
- **שער חוסם** (`#codeGate`, `showCodeGate()`) — **חובה, לא ניתן לדלג**. רץ בעליית האפליקציה אם `!settings.syncCode`. שני מצבים:
  - `new` — קוד חדש: נכשל אם הקוד כבר קיים ("קוד תפוס"), אחרת יוצר מסמך חדש עם הנתונים המקומיים הנוכחיים.
  - `join` — קוד קיים: נכשל אם הקוד לא נמצא, אחרת **מחליף** את כל ה-state המקומי בנתונים מהענן (`normState(remote)`).
  - אחרי הצלחה: `finishCodeGate()` שומר, מפעיל `startCloudSync()`, ורק אז (אם `!wizardDone`) פותח את אשף הפרופיל.
- **סנכרון חי**: `cloudPush()` (debounce 1.2s, בכל `save()`) + `startCloudSync()` (`onSnapshot` — מאזין בזמן אמת לשינויים ממכשירים אחרים עם אותו קוד, מתעדכן אוטומטית ב-UI). השוואת `updatedAt` מונעת לולאת הד על הכתיבה של עצמנו.
- **אל תסיר את פיצ'ר הסנכרון** — המשתמש ביקש אותו במפורש אחרי שהראשון (GitHub Gist, טוקנים) הוסר כי היה מסובך מדי. הפעם זה קוד פשוט בלבד, בלי הרשמה/טוקן/חשבון.
- **אבטחה**: אין Firebase Auth אמיתי — הקוד עצמו הוא "המפתח". דורש חוקי Firestore מתאימים ל-collection הזה (`allow read, write: if true` בטווח `kupaDriverAccounts/{code}`), שהמשתמש צריך להוסיף ידנית ב-Firebase Console (לא ניתן ל-deploy מה-CLI בלי `firebase login` אינטראקטיבי).

## לוגיקה מרכזית (אל תשנה בלי לבדוק)

```js
sums(list) → {inc, com, exp, rides}
sadranDue = rides × pct / 100
net = inc − sadranDue − exp
carDailyCost() = (rent + insuranceMonthly) / workDays   (אם total>0)
```

- `ensureRentExpense()` / `ensureInsuranceExpense()` — מוסיפות הוצאה אוטומטית ב-1 לחודש אם `autoRent`/`autoIns` דלוקים.
- קטגוריות: הכנסה `rides`, `income` · הוצאה `commission`, `rent`, `insurance`, `fuel`, `expense`.
- אשף פרופיל רכב: `openWizard()` (רץ אוטומטית בפתיחה ראשונה כש-`!settings.wizardDone`, וגם מכפתור "עדכון פרופיל" בהגדרות). לא נוגע בנתונים קיימים — משנה רק settings.

## שכבת עיצוב/אנימציה (נוספה 2026-08-23, בלי הכרזה ב"מה חדש")

המשתמש ביקש במפורש "רמה הכי גבוהה" של עיצוב אבל בלי שירגיש עמוס. דברים שכדאי לדעת לפני שנוגעים:
- `openDayPicker()` — רכיב בחירת יום-בחודש (רשת 1-31, מרגיש כמו תאריך אמיתי), משמש גם באשף וגם בהגדרות ל-`billingDay`. **אל תחזירי לזה מקלדת מספרים** — זו הייתה תלונה מפורשת של המשתמש.
- שעת תחילת יום (`dayStartHour`) משתמשת ב-`<input type="time" step="3600">` אמיתי (לא stepper) — גם זו הייתה תלונה מפורשת ("שיהיה כמו שעון").
- `slidePill()` — פונקציה גנרית לפיל נגרר (tab-bar למטה + סגמנט הכנסה/הוצאה למעלה). **חובה `left:0` מפורש ב-CSS** על כל `.tab-pill`/`.seg-pill` חדש — גילינו שבלי זה, ב-flex+RTL הפיל מקבל "עמדה סטטית" שגויה (flex-start = ימין) והתמונה יוצאת מחוץ למסך.
- `.chart-wrap` + class `loaded` — שלד טעינה (shimmer) לגרפים; כל `draw*()` צריך להוסיף `.classList.add('loaded')` על ה-wrap שלו אחרי `setupCanvas` מצליח.
- `renderHeatmap()`/`heatColor()` בדוחות — מפת חום פשוטה (רשת רצפים, לא מיושר לימי שבוע אמיתיים בכוונה, רק "חוזק" הימים).
- נסיעה ישירה לסדרן (`ui.direct`, `#directRow`) ותותים (`cat:'totim'`) כבר תועדו למעלה בסעיף המודל העסקי.
- `pendingRecordGlow` + `#goalCelebrate` — שיא יומי מקבל זוהר עדין על `#tNet`; יעד חודשי מקבל מודאל חגיגה במקום טוסט רגיל.
- `html,body{overflow-x:hidden}` נוסף בכוונה — היה באג אמיתי (לא רק קוסמטי) שגרם למודאלים להיראות לא ממורכזים; אל תסירי את זה.
- `settings.soundOn` (ברירת מחדל **false**) — צליל שמירה נוצר ב-Web Audio API (`playSaveSound()`), אין קובץ שמע חיצוני.
- במכוון **לא** נבנה קרוסלת "סיפור שבועי" או מסך "Wrapped" חודשי — הוחלט שזה כפול למה שכבר יש (גרף שבועי + מפת חום) ומסוכן מדי לקלטר. אם המשתמש מבקש את זה שוב במפורש, זו פיצ'ר חדש נפרד, לא "עוד עיטור".

## תקלות שנתפסו ותוקנו ב-2026-08-26 (לקחים חשובים)

- **`overflow-x:hidden` על html/body שבר גלילה בסמסונג וגם שבר `position:sticky`** (הסכום נעלם בגלילה). זה בדיוק ה-fix שהוספתי בעצמי בסבב הקודם כדי לתקן מודאלים לא ממורכזים — היה רחב מדי. **הפתרון הנכון**: לתקן את מקור החריגה בפועל (היה `.tab-pill`/`.seg-pill` בלי `left:0` מפורש — ראה סעיף למעלה) ולהוסיף `overflow-x:clip` רק על `.app` (לא על html/body) כרשת ביטחון. **אל תחזירי `overflow-x:hidden` ל-html/body** — זה שובר גלילה אמיתית במכשירי אנדרואיד.
- **חסר safe-area בטופבר** — `.topbar` ו-`.amount-box` (ה-sticky top) צריכים `calc(Xpx + env(safe-area-inset-top))`, אחרת השם/הסכום נדבקים ממש לחריץ/שעון באייפון. אם מוסיפים עוד אלמנט sticky/fixed בראש המסך — תמיד תוסיפי את זה.
- **אנימציית `.selpop` (קפיצה בבחירה)** — היתה `scale(1.1)` עם `translateY(-2px)` בלי הרמה חזותית (z-index/צל), וברשת הצפופה של `.cats` זה נראה כמו חפיפה/תקלה. תוקן ל-`scale(1.055)` + צל עדין + `z-index:2` בזמן האנימציה — **אל תגדילי בחזרה מעל ~1.06** בלי לבדוק את רשת הקטגוריות.
- **"ביטול מחיקה" (`showUndo`)** — טעות אמיתית שנתפסה בבדיקה: `.toast` הבסיסי הוא `pointer-events:none` תמיד (כי טוסטים רגילים לא אינטראקטיביים), אז כל טוסט חדש עם כפתור בתוכו (כמו `.undo-toast`) **חייב** `.undo-toast.show{pointer-events:auto}` מפורש, אחרת הכפתור נראה אבל לא לחיץ.
- `state.meta.lastTx` — נשמר בכל שמירת תנועה, משמש ללחיצה ארוכה על ה-FAB (`repeatLastTx()`) כדי לחזור על התנועה האחרונה בלי לפתוח את מסך ההוספה.
- קיצורי דרך (`manifest.json` → `shortcuts`) מפנים ל-`?add=rides`/`?add=fuel`; הטיפול ב-boot קורא את זה **רק** כש-`wizardDone` וכבר יש `syncCode` (לא לנסות לקפוץ להוספה לפני שיש בכלל חשבון), ומנקה את ה-query string אחרי שימוש כדי שרענון/פתיחה הבאה לא "יתקעו" תמיד במסך הוספה.

## מחזור חודשי אישי (`monthStartDay`, נוסף 2026-09-08)

**החודש כבר לא חייב להתחיל ב-1.** זה שינוי עמוק ונוגע כמעט לכל מקום שמציג "חודש" — קרא את הסעיף הזה במלואו לפני שנוגעים בלוגיקת תאריכים.

- `state.settings.monthStartDay` (1-31, ברירת מחדל 1) — היום שבו "החודש" של המשתמש מתחיל. `state.settings.monthCycleSet` (בוליאני) — האם המשתמש כבר נשאל/אישר את זה (נפרד מהערך עצמו, כי ברירת מחדל 1 יכולה להיות גם "לא נשאל עדיין" וגם "בחר 1 במפורש").
- **`{y,m}` בכל מקום (`ui.hm`/`ui.lm`/`ui.rm`) הוא כבר לא "חודש קלנדרי" — זה "אינדקס מחזור"**. משמעות: מחזור `(y,m)` הוא הטווח שמתחיל ביום `monthStartDay` של חודש קלנדרי `m` בשנה `y` (מוצמד לתאריך תקף אם החודש קצר מדי) ומסתיים יום לפני תחילת מחזור `(y,m+1)`. כשה-`monthStartDay===1` זה בדיוק זהה למה שהיה קודם — **אסור שתהיה נסיגה** בהתנהגות ברירת המחדל.
- **פונקציות הליבה** (ליד `monthTx`): `cycleStartDate(y,m)`, `cycleStartISO(y,m)`, `cycleEndISO(y,m)` (בלעדי), `cycleDayCount(y,m)`, `cycleOf(iso)` (מחזיר `{y,m}` של המחזור שתאריך נתון שייך אליו). **`monthTx(y,m)` עברה משוואת `date.slice(0,7)===ymKey` לטווח תאריכים אמיתי (`>=start && <end`) — כל מקום אחר שסינן תנועות/משמרות לפי "אותו חודש" (`hoursInMonth`, קרן, "שבוע מול שבוע" בדוחות) עבר לאותה שיטה. `ymKey(y,m)` עדיין קיימת ותקינה — היא רק מפתח ייחודי (למשל ל-`goalToast` dedup, לשם קובץ CSV), לא סינון תאריכים, so **אל תשתמשי בה לסינון**, רק ל-`cycleStartISO`/`cycleEndISO`/`cycleOf`.
- `monthLabel(o)` — כש-`monthStartDay<=1` מציגה בדיוק את השם הישן ("אוגוסט 2026"); אחרת מציגה טווח ("15 באוגוסט – 14 בספטמבר") דרך `monthLabelShort`/`dfDM`. אם מוסיפים תצוגת חודש חדשה במקום כלשהו — **תמיד** דרך `monthLabel`/`monthLabelShort`, לא `dfMY.format` ישירות.
- **`ensureRentExpense`/`ensureInsuranceExpense` לא קשורות למחזור הזה בכלל** — הן ממשיכות להשתמש ב-`billingDay` וחודש קלנדרי אמיתי לצורך "יורד פעם בחודש", כי זה מתי שהחיוב האמיתי קורה, לא קשור לאיך שהמשתמש בוחר להסתכל על הדוחות שלו. שתי הגדרות נפרדות ומכוונות.
- **גרפים/מפת חום** (`renderHeatmap`, `drawMonth`) עברו מ-`for(i=1;i<=daysInMonth;i++)` להליכה על התאריכים האמיתיים של המחזור (`cycleStartDate` + `cycleDayCount`), ומציגים את יום-בחודש **האמיתי** של כל תא (לא את המיקום היחסי במחזור) — כי מחזור שחוצה שני חודשים קלנדריים לא יכול פשוט למספר 1..n.
- **אשף חד-פעמי למשתמשים קיימים** (`#monthCycleModal`, `maybeShowMonthCycle()`): מוצג **רק** ל-`wizardDone && !monthCycleSet` — נשרשר אחרי `maybeShowWhatsNew()` (אם היא לא מציגה כלום, קוראת ל-`maybeShowMonthCycle()` ישירות; אם היא כן מציגה, `closeWhatsNew()` קוראת לזה אחריה) כדי שהמודלים לא יתנגשו. **משתמש חדש** מקבל `monthCycleSet=true` מיד ב-`cgMode==='new'` (בדיוק כמו `lastSeenVer`) ולעולם לא רואה את זה. **לא נוגע בשום תנועה** — רק בהגדרה.
- **"6000 בכרטיס היום שלך"**: תנועות שנוצרות אוטומטית ע"י `ensureRentExpense`/`ensureInsuranceExpense` מסומנות `lumpAuto:true`. כרטיס "היום שלך" (`dTx` ב-`renderHome`) **מסנן אותן החוצה** מהחישוב (`!x.lumpAuto`) כדי שסכום ענק חד-פעמי לא יעוות את המאזן היומי — אבל **התנועה עצמה נשארת בשלמותה** ב-`state.tx`, מופיעה מלאה במסך "תנועות" (`renderList`), בדוחות, ב-CSV, ובמפת החום. אם יש `lumpAuto` תנועה ביום הנצפה, מוצגת עליה הערה קטנה ושקופה ב-`#tFoot` ("גם ירד היום X — נספר בחישוב החודשי, לא כאן") — **אל תסירי את ההערה הזו**, זו הדרך שהמשתמש יודע שהכסף לא "נעלם", רק לא נספר בכרטיס היומי.

## תיקון באג RTL/מינוס (`bidiNum`, נוסף 2026-09-08)

**כל מקום שמציג סכום עם סימן (+/−) חייב לעבור דרך `bidiNum(...)`.** הבאג שדווח ("המינוס לא במקום") הוא קלאסי ב-RTL: `fmt()`/`fmtInt()` בעצמן כבר עוטפות את התוצאה שלהן ב-`bidiNum()` (עם תווי בידי-איזולציה יוניקוד LRI/PDI, לא CSS), אבל **כל מקום שמוסיף סימן חיצוני** (`'+'+fmt(v)` / `(x>=0?'+':'−')+fmt(x)`) **חייב לעטוף את כל הביטוי המחובר** ב-`bidiNum(...)` בנפרד, אחרת הסימן שנוסף מבחוץ לא מוגן. **אל תנסי לתקן דברים כאלה עם CSS (`direction`/`unicode-bidi` על `.num`)** — כבר ניסינו וזה שבר את היישור של המספרים הגדולים (הזזה שמאלה לא רצויה); הפתרון הנכון היחיד כאן הוא ברמת המחרוזת (JS), לא ברמת ה-CSS.

## כסף קולי (`parseVoiceCommand`, נוסף 2026-09-08)

- כפתור מיקרופון בטופבר (`#voiceBtn`) — **מוסתר כברירת מחדל**, נחשף רק אם `voiceSupported()` (בדיקת `window.SpeechRecognition||webkitSpeechRecognition`) אמת בזמן העלייה. עובד טוב בכרום/אנדרואיד/סמסונג אינטרנט (כולם מבוססי-Chromium); התמיכה בספארי/אייפון פחות ודאית — ההסתרה האוטומטית כבר מטפלת בזה, **אל תניחי שזה עובד בכל דפדפן**.
- `parseVoiceCommand(text)` — כל לוגיקת הפענוח של המשפט העברי. **קריטי: אסור להשתמש ב-`\b` (word boundary) ברגקסים על טקסט עברי** — `\b` ב-JS מוגדר יחסית ל-`\w` (אותיות לועזיות/ספרות בלבד), אותיות עבריות לא נחשבות `\w`, אז `\b` פשוט לא עובד ליד מילים בעברית (נתפס בפועל: `/ביט\b/` פספס "ביט" בסוף משפט). להשתמש בהתאמת מחרוזת פשוטה (`/ביט/`) במקום.
- זיהוי סכום: הרגקס לוקח את המספר הראשון בטקסט, **אחרי** שמסירים ממנו קודם כל התאמת אחוזים (`X% / X אחוז`) — כדי שאחוז עמלה לא ייתפס בטעות כסכום. סדר החילוץ (קודם %, אחר כך סכום מהטקסט שנשאר) חשוב.
- הזרימה: מקשיב → מזהה → מציג אישור ("זיהיתי: הכנסה · ₪250 · נסיעות · ביט") עם הטקסט הגולמי למטה לשקיפות → "שמירה" (שומר ישירות, בלי לפתוח את מסך ההוספה בכלל — עושה זאת ע"י הגדרת `ui.*` הרלוונטיים ואז `$('#saveTx').click()` פרוגרמטית, כדי לעבור דרך **אותה** לוגיקת שמירה בדיוק [שיאים/יעד/direct-pay/הכל] ולא לשכפל אותה) או "לערוך" (פותח את מסך ההוספה עם השדות ממולאים מראש, כמו `quickAdd`, לבדיקה/שינוי ידני לפני שמירה). **אין תמיכה קולית ל"נסיעה ישירה לסדרן"** — לא נבנה בכוונה (מורכב מדי לזיהוי קולי אמין), רק סוג/סכום/קטגוריה/אמצעי תשלום/אחוז עמלה.
- קיצור מ-`manifest.json`: `?voice=1` פותח את המודל אוטומטית בעליית האפליקציה (ולא רק מהכפתור).
- **נבדק ביסודיות**: לוגיקת הפענוח בלבד (13+ ניסוחים שונים, מ-mock אמיתי של ה-API ב-Playwright, לא re-implementation מבודד) — זיהוי קולי אמיתי (מיקרופון/רשת) לא ניתן לבדיקה ב-headless, לא נבדק ידנית מול מיקרופון אמיתי.

## חדשות רכב (נוסף 2026-09-08, גרסה כנה — לא אוטומטית)

המשתמש ביקש עדכוני מחיר דלק + התרעות הפגנות/חוקים "אוטומטי". **נבדק בפועל (חיפוש אמיתי): אין API ציבורי/רשמי למחיר דלק בישראל** — משרד האנרגיה מפרסם הודעה חודשית (טקסט/פייסבוק), לא feed. בהתאם לכך, מה שנבנה בפועל בכרטיס "חדשות רכב וכביש" (מסך דוחות) הוא **גרסה כנה, לא אוטומטית**: מחיר דלק כמספר קבוע שמתעדכן ידנית (יש הערה מפורשת בתוך הכרטיס שאומרת את זה למשתמש — **אל תסירי את ההערה**), ושני קישורים מהירים אמיתיים ושימושיים (Google Maps לתנועה, אתר משרד התחבורה). **אם המשתמש מבקש שוב אוטומציה/התרעות אמיתיות פה** — זה דורש שרת (Cloud Function בתזמון + Firestore + FCM), בדיוק כמו שהוסבר לו לגבי המפה/הבוט; אל תבני משהו שמתחזה לאוטומטי בלי שזה קיים באמת.

## כללים כשרוצים לשנות

1. שמור על RTL ועברית תקינה.
2. אל תעשה את ה-UI צבעוני/ילדותי.
3. הוסף אייקונים חדשים ל-`sw.js` ב-`ASSETS`.
4. אחרי שינוי משמעותי — הגדל את שמות הקבצים המותאמים (cache name ב-`sw.js`) כדי שמשתמשים מותקנים יקבלו עדכון.
5. שמור על תאימות מובייל (טלפון ראשון).
6. אם אתה מוסיף שדה ל-State — הוסף ברירת מחדל ב-`normState(d)`.

## פריסה

הפרויקט נפרס ל-GitHub Pages. המדריך המלא בקבצים: `guide.html` ו-`README.md`.

**עדכון 2026-08-31: הפריסה עברה מ-"legacy" (branch) ל-"workflow" (GitHub Actions)** — `build_type` שונה דרך `gh api -X PUT repos/.../pages -f build_type=workflow`, כי פיצ'ר ההפצה (למטה) דורש קובץ שנוצר בזמן build מתוך secret ולא נמצא בגיט. `git push` ל-`main` עדיין מפעיל דיפלוי אוטומטית — עכשיו דרך `.github/workflows/deploy.yml`, לא דרך ה-API הישן של `pages/builds`. **אם אי פעם צריך לוודא שדיפלוי הסתיים, תבדקי `gh run list --workflow=deploy.yml` ולא את ה-Pages builds API הישן.**

## פיצ'ר "הפצת נסיעה" (מוסתר — feature flag, נוסף 2026-08-31)

פיצ'ר אישי ומוסתר לגמרי, נבנה במפורש כך שהוא **ידני בלבד — אין אוטומציה, אין שרת שולח הודעות**. המשתמש מעתיק הודעה ולוחץ שלח בעצמו בכל קבוצה, בדיוק כמו `shareSummary()` שכבר קיימת (wa.me). **אל תוסיפי שום דבר ששולח לבד** — זו הייתה דרישה מפורשת ומכוונת של המשתמש אחרי שסירבתי לבנות גרסה מבוססת-בוט (Baileys) בגלל הפרת תנאי שימוש של וואטסאפ.

- **Feature flag** (`isBroadcastAllowed()`): בודק אם `state.settings.syncCode` נמצא ב-`window.BROADCAST_ALLOWLIST`. המשתנה הזה **לא בקוד המקור** — נטען מ-`bc-config.js` (`<script src="bc-config.js" onerror="...">` בראש ה-HTML, לפני הסקריפט הראשי). הקובץ **לא נמצא ב-git** (יש ב-`.gitignore`) ונוצר רק בזמן דיפלוי מ-GitHub Actions secret בשם `BROADCAST_ALLOWLIST` (רשימה מופרדת בפסיקים) — ראה `.github/workflows/deploy.yml`. **להוספת משתמש מורשה בעתיד**: לעדכן את ה-secret ב-GitHub (Settings → Secrets and variables → Actions → `BROADCAST_ALLOWLIST`) עם קודים נוספים מופרדים בפסיק, לא לגעת בקוד.
  - **מגבלה אמיתית שהוסברה למשתמש**: זה אתר סטטי בלי שרת — גם עם המנגנון הזה, `bc-config.js` בפועל הוא קובץ ציבורי בכתובת ידועה. זה מוציא את הקוד מהיסטוריית git ומדפדפן הרפו הציבורי, אבל לא הופך אותו לסוד אמיתי. אל תבטיחי סודיות מוחלטת אם שואלים.
  - לבדיקה מקומית: יוצרים `bc-config.js` ידני (מחוץ לגיט) עם הקוד האמיתי לבדיקות Playwright.
- **UI מוסתר לגמרי**: `#bcTab` (טאב תחתון) ו-`#bcSettingsCard` (בהגדרות) מתחילים עם `display:none` ב-HTML, ונחשפים רק דרך JS ב-`applyFeatureFlags()` (נקראת מ-`renderSettings()`, שכבר רץ בעליית האפליקציה ואחרי `finishCodeGate()`). ב-`go(id)`: אם `id==='broadcast'` ו-`!isBroadcastAllowed()` — מפנה ל-`home` בשקט, כהגנה נוספת (לא אמיתית — מי שפותח devtools ומשנה classList ידנית עדיין יכול לראות; זו הגנת UI רגילה, לא אבטחה).
- **State**: `state.bcGroups` (מערך `{id,name,link,fav}`), `state.bcHistory` (מערך `{id,text,ts}`, מוגבל ל-20 אחרונות), `state.bcSession` (`{text,sentIds}` — עוקב אחרי אילו קבוצות כבר "נשלח" עבור ההודעה הנוכחית; מתאפס אוטומטית כשהטקסט משתנה, או ידנית עם `#bcResetSent`). `state.settings.bcSignature` (ברירת מחדל: `'לבקש באישי בלבד'`).
- **מנגנון שליחה** (`$('#bcGroupList')` click handler): מעתיק ללוח (`copyToClipboard()`, עם fallback ל-`execCommand('copy')` לדפדפנים ישנים) ואז `window.open(g.link)` לקישור ההזמנה של הקבוצה. **אין `wa.me` עם `?text=` לקבוצות** — זה עובד רק לצ'אט אישי, לא לקבוצה קיימת, ולכן חובה להעתיק+לפתוח בנפרד ולתת למשתמש להדביק.
- קישורי קבוצה מאומתים ב-`bcFormSave` שמתחילים ב-`https://chat.whatsapp.com/` בלבד.
- נבדק: משתמש לא ברשימה לא רואה כלום; ההודעה המועתקת כוללת את החתימה במלואה; פתיחת קבוצה פותחת את הקישור הנכון; הכול ב-Playwright מול Firestore אמיתי (קוד בדיקה נמחק בסוף).

## עדכון 2026-09-01: נוספה שליחה אוטומטית אמיתית (בוט Baileys) — היפוך מכוון של הכלל למעלה

המשתמש חזר וביקש במפורש בוט אמיתי ששולח לבד, **אחרי שהוסבר לו שוב** שזה מפר את תנאי השימוש של וואטסאפ ומסכן את המספר בחסימה. הוא אישר את הסיכון במפורש ("בכל זאת אני רוצה בוט אמיתי ששולח לבד"). **זו החלטה מודעת שלו, לא טעות שצריך לתקן** — אל תחזרי להסיר את זה רק כי הכלל הישן למעלה אומר "אין אוטומציה". אם המשתמש מבקש שוב לבטל את זה או להתלונן על חסימה, זה הזמן להחזיר להצעת המנגנון הידני, לא לפני כן.

- **הבוט עצמו לא בריפו הזה** — פרויקט נפרד: `https://github.com/neorai1122/wa-bot` (Node + `@whiskeysockets/baileys` + Express). מריץ WhatsApp Web כ"מכשיר מקושר" (QR scan פעם אחת, נשמר בתיקיית `auth/`). Endpoints: `GET /` (סטטוס), `GET /qr` (עמוד סריקה), `GET /groups` (רשימת קבוצות, דורש `x-api-key`), `POST /broadcast` (`{message, groupIds[]}`, דורש `x-api-key`, שולח בהפרש 1.5 שנייה בין הודעות כדי להקטין סיכון חסימה).
- **איפה זה רץ**: **על המחשב האישי של המשתמש** (לא בענן) — ניסיון Railway נכשל כי נגמר הטריאל בחינם, וה-CLI חוסם login לא-אינטראקטיבי מטרמינל (הגנת אבטחה מכוונת של Railway, אין דרך לעקוף מסקריפט). המשתמש ביקש להישאר על המחשב בינתיים ולעבור לענן בהמשך "לראות איך זה עובד קודם". **כשעוברים לענן**: לזכור להעביר גם את `AUTH_DIR` (session) ולוודא volume קבוע, אחרת המשתמש יצטרך לסרוק QR מחדש.
- **חיבור מהאפליקציה**: `state.settings.botUrl` + `state.settings.botKey` (ברירת מחדל `''`, בתוך `bcSettingsCard` המוסתר, לא ב-`WHATS_NEW` — נשאר בלתי-נראה למי שלא ברשימה, כמו כל הפיצ'ר). `botReady()`, `botBaseUrl()`, `botSend(waId,text)` — helpers חדשים ליד `bcSigText()`.
- **קבוצות**: ל-`bcGroups` נוסף שדה אופציונלי `waId` (מזהה `...@g.us` אמיתי מוואטסאפ, שונה מ-`id` הפנימי של האפליקציה). כפתור "טעינת קבוצות מהבוט" (`#bcImportBotBtn`, בתוך מודל ניהול קבוצות) קורא ל-`GET /groups` וממזג לפי `waId` (לא דורס קבוצות ידניות קיימות). קבוצה עם `waId` וגם בוט מוגדר ומחובר מוצגת עם 🤖 ו"שליחה אוטומטית" — הלחיצה שולחת ישירות דרך `POST /broadcast`, בלי clipboard ובלי `window.open`. קבוצה בלי `waId` (או בלי בוט מוגדר) ממשיכה בזרימה הידנית הישנה (`link`, העתקה+פתיחה).
- **בדיקת חיבור**: `#botTestBtn` בהגדרות קורא ל-`GET /` (לא דורש מפתח) ומציג "מחובר ✓" / "ממתין לסריקת QR" / "הבוט לא מחובר" / שגיאת רשת.
- **לא נבדק עדיין ב-Playwright מול בוט אמיתי** (הבוט רץ מקומית על מחשב המשתמש, לא נגיש מסביבת הבדיקה) — נבדק ידנית מול ה-API של הבוט עם curl בלבד (חיבור, `/groups`, תשובת שגיאה עם מפתח שגוי). אם משנים את הזרימה הזו, כדאי לבדוק ידנית מול בוט אמיתי, לא להסתמך רק על בדיקת syntax.

## עדכון 2026-09-09: "עדכון פסיכי" — נעילה, שיתוף משפחה, הסיפור שלי, מצב רכב, מפת חום שני-מימדית, מנוע יומי (v10)

המשתמש ביקש סבב גדול של רעיונות "וואו" (קסם אוטומטי + חוויה חושית, בלי עלות, בלי כאב ראש רב) ואז ביקש לבנות את כולם בבת אחת "ברמה הכי גבוהה... שלא יראה מפוצץ". **הוחלט במפורש לא לבנות הכול פשוטו כמשמעו** — שני רעיונות הוצאו מהיקף הבנייה מסיבות מפורשות שהוסברו למשתמש, ולא נבנו:
- **סריקת קבלה במצלמה (OCR)** — דורש ספרייה חיצונית (כמו Tesseract.js), שובר את העיקרון של "קובץ יחיד, בלי build step"; דיוק לא אמין על קבלות מצולמות בעברית.
- **זיהוי אוטומטי של תחילת/סוף נסיעה לפי תנועת הטלפון** — הרשאות רקע מורכבות, סוללה, הרבה false positives (נוסע ברכב כנוסע ולא כנהג). הוחלט לחכות להחלטה משותפת עם המשתמש אם ירצה בכך.
גם "טיקר חי בהתראות" (התראה שמתעדכנת לאורך היום) ו"מסע בזמן אחורה" (מסך memory-lane נפרד) לא נבנו — נשקלו כמיותרים/מיותר-קרובים לפיצ'רים קיימים (חצי ניווט בין ימים כבר קיים) ומסוכנים ל"עומס התראות" בדיוק מה שהמשתמש לא רצה.

מה שכן נבנה, בקצרה (כל הפיצ'רים הבאים חינמיים לגמרי — משתמשים רק ב-Firebase שכבר קיים, ב-API-ים מובנים בדפדפן, בלי שרת חדש, בלי כרטיס אשראי):

### מנוע יומי (בוקר-ערב-שבועי) — `checkMorning()`, `checkWeekly()`, `checkOverwork()`
- `state.settings.morningOn`/`morningTime` (ברירת מחדל: כבוי, 08:00) — התראה מקומית (`Notification`, לא push אמיתי משרת) עם: נטו אתמול, מרחק ליעד, רצף, ו"נבואה" (`weekdaySuccessChance(wd,avg)` — % מהימים ההיסטוריים באותו יום בשבוע שעברו את הממוצע היומי הכללי, `avgDailyNet()`).
- `checkWeekly()` — כל יום ראשון (אחרי שעת הבוקר טוב), משווה שבוע שעבר מול השבוע שלפניו, גייטד ב-`state.meta.weekShown` (תאריך).
- `checkOverwork()` — משווה שעות השבוע הנוכחי (`weeklyHoursEndingToday`) מול ממוצע 6 השבועות הקודמים; מתריע רק אם יש **לפחות 3 שבועות עבר עם נתונים** (אחרת שקט, לא ממציא נתון) והשבוע הנוכחי גבוה ב-30%+. גייטד ב-`state.meta.overworkWeek` (מפתח = תחילת השבוע, `weekStartISO`).
- **כל אלה מקומיות בלבד** (לא FCM/push אמיתי) — עובדות רק כשהאפליקציה פתוחה/ברקע קרוב, בדיוק כמו התזכורת הישנה `checkNotify()`. שדרוג לpush אמיתי דורש Firebase Blaze (כרטיס אשראי) — הוסבר למשתמש והוא בחר בגרסה החינמית.

### מנוע אבני דרך + חגיגות — `checkMilestones()`, `celebrate()`
- `checkMilestones(tx,d)` נקרא מ-`saveTx` אחרי כל שמירה, בודק (בסדר עדיפות): סכום מצטבר עגול (1,000 עד מיליון, ב-`state.meta.milestones['life_'+th]`), רצף ימים (7/14/30/60/100, `'streak_'+sd`), שיא אישי ליום בשבוע הספציפי (`'wd_'+wd`, נשמר כתאריך כדי לאפשר עדכון). **כל אבן דרך נחגגת פעם אחת בלבד** — הדגלים ב-`state.meta.milestones` לא מתאפסים לעולם.
- `celebrate(emoji,title,sub)` — הכליל את המודל הישן `#goalCelebrate` (שהיה רק ליעד חודשי) לשימוש כללי: גשם מטבעות אמיתי (`spawnCoinRain()`, DOM אמוג'י מונפשים ב-CSS, לא canvas) + צליל "צלצול" עולה (`playCelebrationSound()`, Web Audio, 4 טונים). בשימוש כרגע ב-3 מקומות ב-`saveTx`: הגעה ליעד (עדיפות ראשונה), אבן דרך (שנייה), שיא יומי חדש (שלישית) — **רק אחד מוצג בכל שמירה**, לא נערמים.

### קול — `speak()`, כפתור השמעת יום, ברכת בוקר
- `speak(text)` — עטיפה סביב `SpeechSynthesis` המובנה בדפדפן (`he-IL`), מכבד את `state.settings.soundOn`. **דורש ז'סטורת משתמש בהרבה דפדפנים (בעיקר ספארי/אייפון)** — קריאה אוטומטית מה-boot (ברכת הבוקר) עלולה להיכשל בשקט (try/catch), זה בסדר ומכוון, לא לתקן ל"תמיד עובד".
- כפתור רמקול בכרטיס "היום שלך" (`#speakDayBtn`, מוצג רק כשצופים בהיום הנוכחי) קורא ל-`daySummaryCache` (מתעדכן בכל `renderHome()`) ומקריא הכנסה/הוצאה/רכב/מאזן.
- ברכת בוקר: בעליית האפליקציה, פעם ביום (`state.meta.morningGreeted`), אחרי 1.2 שנ' השהיה.

### נעילת פרטיות — `#lockGate`
- **לא אבטחה אמיתית** — קוד 4 ספרות נשמר גלוי ב-`state.meta.lockPin` (חלק מה-state שמסונכרן ל-Firestore ולגיבוי JSON). זו "מגן חברתי" בלבד (מישהו שמושיט יד לטלפון), לא הגנה מפני מי שיש לו גישה לנתונים בכל מקרה. **אם המשתמש ירצה אבטחה אמיתית בעתיד — זה דורש ארכיטקטורה אחרת** (הצפנה בצד לקוח לפני שמירה, לא רק שכבת UI).
- נבדק חוזר: הגדרה → נעילה בטעינה חוזרת → קוד שגוי מציג שגיאה → קוד נכון פותח ושומר ב-`sessionStorage`.

### שיתוף משפחה — `pushPublicShare()`, `share.html`
- קולקציית Firestore נפרדת `publicShare/{shareId}` (לא `kupaDriverAccounts`) — מכילה **רק** `{name,net,hours,streak,updatedAt}` של היום הנוכחי, בלי שום פירוט הכנסות/הוצאות/יעדים. `shareId` נוצר פעם אחת (`state.meta.shareId`), לא הקוד האישי עצמו.
- `share.html` — עמוד ציבורי נפרד לגמרי (לא חלק מה-PWA המותקן, לא ב-`sw.js` ASSETS), קורא ל-Firestore ישירות דרך אותו `firebaseConfig`, `onSnapshot` בזמן אמת. אין בו סנכרון, אין התחברות, אין קוד אישי — קישור-צפייה בלבד.
- **חוקי Firestore פתוחים לגמרי** (`allow read, write: if true`) כמו שאר הפרויקט — אם אי פעם מהדקים את החוקים, `publicShare` צריך `allow read: if true; allow write: if request.resource.data.keys().hasOnly(['name','net','hours','streak','updatedAt'])` בערך, לא לפתוח כתיבה חופשית.

### מצב תצוגה לרכב — `#carModeOverlay`, `updateCarMode()`
- מגיב ל-`matchMedia('(orientation: landscape)')` — מוצג אוטומטית כשהטלפון לרוחב **וגם** `state.settings.carModeOn` (ברירת מחדל: מופעל) **וגם** `ui.screen==='home'`. נסגר בנגיעה או בחזרה לאורך. **לא חוסם מסכים אחרים בלרוחב** (דוחות/רשימה) — נבדק בכוונה, כדי לא להפריע לצפייה בגרפים לרוחב.

### מפת חום שני-מימדית — `renderWeekHourHeat()`
- שונה מ"מפת חום — איך היה כל יום" הקיימת (זו לפי תאריך בלוח שנה) — זו לפי **יום בשבוע × שעה ביום** (`t.ts` של כל תנועת הכנסה, מחולק ל-6 בלוקים של 4 שעות). מוסתרת לגמרי (`display:none`) אם אין עדיין נתוני `ts` מספיקים (משתמש חדש).

### הסיפור שלי — `openStory()`, `#storyModal`
- מסך "סטורי" מלא (כמו Instagram/Spotify Wrapped) שנבנה מההיסטוריה המלאה של המשתמש — לא מוגבל לשנה קלנדרית (למרות השם "שנתי" שעלה בשיחה, בפועל זה "כל הזמנים" כי משתמש בודד לא בהכרח צבר שנה שלמה של נתונים). ניווט בנגיעה בצדדים (ימין=הבא, שמאל=הקודם, מוסבר בקוד כי `left`/`right` ב-CSS הם פיזיים ולא לוגיים, אז זה עובד נכון גם ב-RTL).

**גרסה: APP_VER=10, `sw.js` CACHE=kupa-v3-14.**
