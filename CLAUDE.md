# CLAUDE.md — הנחיות עבודה על קופת הנהג

פרויקט זה הוא אפליקציית ווב עברית (PWA) לניהול כסף של נהגי "דריבר" בישראל.
קלוד, עבוד על זה לפי ההנחיות הבאות.

## מי המדריך / בעל האפליקציה

- נהג דריבר ישראלי, עובד מהטלפון, רוצה פשטות וניקיון.
- קורא בעברית. כתוב תשובות והסברים בעברית, בסגנון ישיר, חם, קולע — בלי קישקושים.
- מעצב סגנון: אפליקציית פינטק מקצועית — **נקי, דק, חלק, יוקרתי, מינימלי. ללא צבעים מרובים, ללא אלמנטים ילדותיים**.

## המודל העסקי (חשוב! אל תשבור אותו)

- **סדרן**: מסדר נסיעות ומקבל **אחוז מכל נסיעה — לא בהכרח קבוע!** לנהגים שונים (ולפעמים לאותו נהג, נסיעה-נסיעה) יכול להיות אחוז שונה (למשל 10% מתחנה אחת, 30% מתחנה אחרת) — תלוי בחוזה מול התחנה/סדרן.
  - `settings.pct` הוא רק **ברירת המחדל** (מוצג בהגדרות). כל תנועת `rides` בפועל יכולה לשאת `tx.pct` משלה שדורס את ברירת המחדל.
  - `ridePct(t)` — הפונקציה היחידה שקובעת איזה % חל על תנועה: `t.pct!=null?t.pct:state.settings.pct`. **תמיד להשתמש בה**, לא ב-`state.settings.pct` ישירות, בכל מקום שמחשב עמלה/נטו לתנועת rides ספציפית.
  - `sums(list)` כבר מצרפת נכון per-transaction (`due` = סכום עמלות לפי ה-% של כל נסיעה בנפרד) — אל תחזרי לחישוב `rides_total * pct_גלובלי`.
  - UI: `#pctRow`/`#pctChips` במסך ההוספה (רק כש-`addType==='income' && addCat==='rides'`) — צ'יפים 10/20/25/30% + "אחר" (openSheet). הבחירה האחרונה נשמרת ב-`settings.lastRidePct` ומוצעת כברירת מחדל בפעם הבאה.
  - דוגמה: נסיעה אשדוד–בני ברק ₪180 בעמלה 30% → סדרן ₪54, נהג ₪126. אותה נסיעה בעמלה 10% → סדרן ₪18, נהג ₪162.
- **רכב**: 4 מצבי בעלות אפשריים ב-`settings.ownershipType`, נבחרים באשף הגדרה:
  - `rent` (שכירות) — תשלום חודשי קבוע, ביטוח בנפרד.
  - `finance` (מימון/הלוואה) — **שונה מליסינג!** החזר חודשי על הרכב + ביטוח משולם בנפרד לגמרי (שני שדות, שני סכומים).
  - `leasing` (ליסינג) — **שונה ממימון!** תשלום חודשי אחד שכולל הכל (ביטוח, טיפולים וכו') חוץ מדלק. באשף מדלגים אוטומטית על שלב הביטוח (`insuranceType='included'`, `insuranceMonthly=0`), ובהגדרות שורות הביטוח מוסתרות (`#insRow`,`#autoInsRow` עם `display:none`).
  - `owned` (בבעלות) — אין תשלום חודשי על הרכב עצמו, אבל עדיין יש ביטוח בנפרד.
  - שדה `settings.rent` הוא **הסכום החודשי הכולל** של שכירות/מימון/ליסינג (תלוי סוג) — ברירת מחדל **0**, לעולם לא ערך מומצא.
- `settings.billingDay` (1-31, ברירת מחדל 1) — יום החיוב בפועל בחודש (רלוונטי רק ל-rent/finance/leasing, לא ל-owned). נבחר באשף (`wizStep2b`) ובהגדרות (`data-act="set-billingday"`). `ensureRentExpense()` משתמשת בו לתאריך ההוצאה האוטומטית במקום להניח תמיד "1 לחודש" (`Math.min(billingDay, daysInMonth(y,m))` כדי לא לחרוג מסוף חודשים קצרים). שינוי הערך משפיע רק על חודשים עתידיים — לא נוגע בתנועות היסטוריות קיימות.
- **ביטוח**: `settings.insuranceMonthly` + `settings.insuranceType` (חובה/צד ג׳/מקיף/included). רלוונטי לכל הסוגים חוץ מ-leasing.
  - המספרים משתנים בין נהגים — אסור לתפור ערכים קבועים; חייב להיות ניתן לשינוי בהגדרות ובאשף.
  - `carCostBasisLbl()` — מחזיר תווית תצוגה מתאימה ("רכב+ביטוח" או "ליסינג, כולל הכל") לפי הסוג.
- רווח נטו = ברוטו − עמלת סדרן − הוצאות − עלות רכב יומית (`carDailyCost()` = (rent+insuranceMonthly)/workDays).
- עמלת סדרן מחושבת **רק על קטגוריית "נסיעות" (rides)**, לא על הכנסות אחרות.
- **נסיעה ששולמה ישירות לסדרן** (`ui.direct`, מתג `#directRow`/`#directSwitch` שמופיע רק כש-`isRideCat`): הלקוח שילם לסדרן ישירות, הנהג לא רואה מזומן. ב-`saveTx`: נרשמת תנועת ההכנסה הרגילה עם `tx.method='direct'`, ובמקביל **אוטומטית** נוצרת תנועת הוצאה מקושרת `{cat:'commission', amount: מלוא סכום הנסיעה}` (לא רק העמלה!) עם `linkedTx` שמצביע לנסיעה. זה בכוונה — כך `sums()` הקיימת (`com+=amount` לכל תנועת commission) כבר "מכסה" גם את העמלה של הנסיעה הזו וגם משאירה יתרה שמפחיתה חוב ישן, בלי צורך בשדות חדשים בלוגיקת הסיכום. **אל תשנה את זה ל"רק אחוז העמלה"** — המשתמש אישר במפורש שהסכום המלא יורד מהחוב.
- `METHODS.direct`='ישיר לסדרן' — נכנס אוטומטית לפירוט אמצעי תשלום בדוחות ולתגית ברשימת התנועות (קוד גנרי קיים, לא נדרש טיפול מיוחד).
- **תותים** (`cat:'totim'`, קטגוריית הוצאה, אייקון 🍓) — ניכוי קטן ולא קבוע שהסדרן מוריד מדי פעם. **זה מינוס, לא בונוס!** נספר בדיוק כמו כל הוצאה אחרת ב-`sums()` (`exp+=amount`) — אין צורך בלוגיקה מיוחדת.
- `settings.dayStartHour` (0-23, ברירת מחדל 0) — שעת תחילת "יום" עבור המשתמש (למשל 6 = יום מתחיל ב-6 בבוקר, לא בחצות). `todayISO()` היא **המקור היחיד** לחישוב "היום" בכל האפליקציה ומתחשבת בזה — אל תחזרי לחשב `new Date()` ישירות במקומות שאמורים לשקף את "היום הלוגי" של המשתמש.
- **"מה חדש"** (`APP_VER`, `WHATS_NEW`, `maybeShowWhatsNew()`): פופאפ שמוצג פעם אחת לכל גרסה, רק למשתמשים ש-`wizardDone` (לא למשתמש חדש שרק נרשם — אצלו `lastSeenVer` נקבע ל-`APP_VER` מיד ב-`cgMode==='new'`). **בכל שינוי משמעותי: הגדל את `APP_VER` ב-1 ועדכן את מערך `WHATS_NEW`** (2-4 שורות קצרות וברורות, לא טכניות) — אחרת המשתמשים לא יראו שהיה עדכון.

## מבנה קבצים

| קובץ | תפקיד |
|------|-------|
| `index.html` | האפליקציה כולה (HTML/CSS/JS מוטבע). גרסה 2.2 |
| `manifest.json` | מניפסט PWA |
| `sw.js` | Service Worker (אופליין + עדכונים) |
| `guide.html` | מדריך התקנה וענן בעברית |
| `.github/workflows/deploy.yml` | דיפלוי ל-GitHub Pages |
| `icon-192/512/180/maskable.png` | אייקונים |

## אחסון וסנכרון ענן (קוד אישי)

- **לוקלי**: `localStorage` מפתח `kupaNehed.v2` — תמיד מקור האמת המיידי במכשיר.
- **ענן**: Firebase Firestore, פרויקט קיים ומשותף עם אפליקציות אחרות של המשתמש (`sodg-272b7`) — **collection ייעודי ונפרד**: `kupaDriverAccounts`. אסור לגעת/לקרוא collections אחרים באותו פרויקט.
- **מנגנון**: כל משתמש בוחר "קוד אישי" חופשי (`settings.syncCode`, מינ' 4 תווים). מסמך Firestore: `kupaDriverAccounts/{code}` עם שדה `data` = כל ה-state כ-JSON string, ו-`updatedAt` (serverTimestamp, מטא בלבד — ההשוואה האמיתית היא על `state.updatedAt` הפנימי).
- **שער חוסם** (`#codeGate`, `showCodeGate()`) — **חובה, לא ניתן לדלג**. רץ בעליית האפליקציה אם `!settings.syncCode`. שני מצבים:
  - `new` — קוד חדש: נכשל אם הקוד כבר קיים ("קוד תפוס"), אחרת יוצר מסמך חדש עם הנתונים המקומיים הנוכחיים.
  - `join` — קוד קיים: נכשל אם הקוד לא נמצא, אחרת **מחליף** את כל ה-state המקומי בנתונים מהענן (`normState(remote)`).
  - אחרי הצלחה: `finishCodeGate()` שומר, מפעיל `startCloudSync()`, ורק אז (אם `!wizardDone`) פותח את אשף הפרופיל.
- **סנכרון חי**: `cloudPush()` (debounce 1.2s, בכל `save()`) + `startCloudSync()` (`onSnapshot` — מאזין בזמן אמת לשינויים ממכשירים אחרים עם אותו קוד, מתעדכן אוטומטית ב-UI). השוואת `updatedAt` מונעת לולאת הד על הכתיבה של עצמנו.
- **אל תסיר את פיצ'ר הסנכרון** — המשתמש ביקש אותו במפורש אחרי שהראשון (GitHub Gist, טוקנים) הוסר כי היה מסובך מדי. הפעם זה קוד פשוט בלבד, בלי הרשמה/טוקן/חשבון.
- **אבטחה**: אין Firebase Auth אמיתי — הקוד עצמו הוא "המפתח". דורש חוקי Firestore מתאימים ל-collection הזה (`allow read, write: if true` בטווח `kupaDriverAccounts/{code}`), שהמשתמש צריך להוסיף ידנית ב-Firebase Console (לא ניתן ל-deploy מה-CLI בלי `firebase login` אינטראקטיבי).

## לוגיקה מרכזית (אל תשנה בלי לבדוק)

```js
sums(list) → {inc, com, exp, rides}
sadranDue = rides × pct / 100
net = inc − sadranDue − exp
carDailyCost() = (rent + insuranceMonthly) / workDays   (אם total>0)
```

- `ensureRentExpense()` / `ensureInsuranceExpense()` — מוסיפות הוצאה אוטומטית ב-1 לחודש אם `autoRent`/`autoIns` דלוקים.
- קטגוריות: הכנסה `rides`, `income` · הוצאה `commission`, `rent`, `insurance`, `fuel`, `expense`.
- אשף פרופיל רכב: `openWizard()` (רץ אוטומטית בפתיחה ראשונה כש-`!settings.wizardDone`, וגם מכפתור "עדכון פרופיל" בהגדרות). לא נוגע בנתונים קיימים — משנה רק settings.

## שכבת עיצוב/אנימציה (נוספה 2026-08-23, בלי הכרזה ב"מה חדש")

המשתמש ביקש במפורש "רמה הכי גבוהה" של עיצוב אבל בלי שירגיש עמוס. דברים שכדאי לדעת לפני שנוגעים:
- `openDayPicker()` — רכיב בחירת יום-בחודש (רשת 1-31, מרגיש כמו תאריך אמיתי), משמש גם באשף וגם בהגדרות ל-`billingDay`. **אל תחזירי לזה מקלדת מספרים** — זו הייתה תלונה מפורשת של המשתמש.
- שעת תחילת יום (`dayStartHour`) משתמשת ב-`<input type="time" step="3600">` אמיתי (לא stepper) — גם זו הייתה תלונה מפורשת ("שיהיה כמו שעון").
- `slidePill()` — פונקציה גנרית לפיל נגרר (tab-bar למטה + סגמנט הכנסה/הוצאה למעלה). **חובה `left:0` מפורש ב-CSS** על כל `.tab-pill`/`.seg-pill` חדש — גילינו שבלי זה, ב-flex+RTL הפיל מקבל "עמדה סטטית" שגויה (flex-start = ימין) והתמונה יוצאת מחוץ למסך.
- `.chart-wrap` + class `loaded` — שלד טעינה (shimmer) לגרפים; כל `draw*()` צריך להוסיף `.classList.add('loaded')` על ה-wrap שלו אחרי `setupCanvas` מצליח.
- `renderHeatmap()`/`heatColor()` בדוחות — מפת חום פשוטה (רשת רצפים, לא מיושר לימי שבוע אמיתיים בכוונה, רק "חוזק" הימים).
- נסיעה ישירה לסדרן (`ui.direct`, `#directRow`) ותותים (`cat:'totim'`) כבר תועדו למעלה בסעיף המודל העסקי.
- `pendingRecordGlow` + `#goalCelebrate` — שיא יומי מקבל זוהר עדין על `#tNet`; יעד חודשי מקבל מודאל חגיגה במקום טוסט רגיל.
- `html,body{overflow-x:hidden}` נוסף בכוונה — היה באג אמיתי (לא רק קוסמטי) שגרם למודאלים להיראות לא ממורכזים; אל תסירי את זה.
- `settings.soundOn` (ברירת מחדל **false**) — צליל שמירה נוצר ב-Web Audio API (`playSaveSound()`), אין קובץ שמע חיצוני.
- במכוון **לא** נבנה קרוסלת "סיפור שבועי" או מסך "Wrapped" חודשי — הוחלט שזה כפול למה שכבר יש (גרף שבועי + מפת חום) ומסוכן מדי לקלטר. אם המשתמש מבקש את זה שוב במפורש, זו פיצ'ר חדש נפרד, לא "עוד עיטור".

## תקלות שנתפסו ותוקנו ב-2026-08-26 (לקחים חשובים)

- **`overflow-x:hidden` על html/body שבר גלילה בסמסונג וגם שבר `position:sticky`** (הסכום נעלם בגלילה). זה בדיוק ה-fix שהוספתי בעצמי בסבב הקודם כדי לתקן מודאלים לא ממורכזים — היה רחב מדי. **הפתרון הנכון**: לתקן את מקור החריגה בפועל (היה `.tab-pill`/`.seg-pill` בלי `left:0` מפורש — ראה סעיף למעלה) ולהוסיף `overflow-x:clip` רק על `.app` (לא על html/body) כרשת ביטחון. **אל תחזירי `overflow-x:hidden` ל-html/body** — זה שובר גלילה אמיתית במכשירי אנדרואיד.
- **חסר safe-area בטופבר** — `.topbar` ו-`.amount-box` (ה-sticky top) צריכים `calc(Xpx + env(safe-area-inset-top))`, אחרת השם/הסכום נדבקים ממש לחריץ/שעון באייפון. אם מוסיפים עוד אלמנט sticky/fixed בראש המסך — תמיד תוסיפי את זה.
- **אנימציית `.selpop` (קפיצה בבחירה)** — היתה `scale(1.1)` עם `translateY(-2px)` בלי הרמה חזותית (z-index/צל), וברשת הצפופה של `.cats` זה נראה כמו חפיפה/תקלה. תוקן ל-`scale(1.055)` + צל עדין + `z-index:2` בזמן האנימציה — **אל תגדילי בחזרה מעל ~1.06** בלי לבדוק את רשת הקטגוריות.
- **"ביטול מחיקה" (`showUndo`)** — טעות אמיתית שנתפסה בבדיקה: `.toast` הבסיסי הוא `pointer-events:none` תמיד (כי טוסטים רגילים לא אינטראקטיביים), אז כל טוסט חדש עם כפתור בתוכו (כמו `.undo-toast`) **חייב** `.undo-toast.show{pointer-events:auto}` מפורש, אחרת הכפתור נראה אבל לא לחיץ.
- `state.meta.lastTx` — נשמר בכל שמירת תנועה, משמש ללחיצה ארוכה על ה-FAB (`repeatLastTx()`) כדי לחזור על התנועה האחרונה בלי לפתוח את מסך ההוספה.
- קיצורי דרך (`manifest.json` → `shortcuts`) מפנים ל-`?add=rides`/`?add=fuel`; הטיפול ב-boot קורא את זה **רק** כש-`wizardDone` וכבר יש `syncCode` (לא לנסות לקפוץ להוספה לפני שיש בכלל חשבון), ומנקה את ה-query string אחרי שימוש כדי שרענון/פתיחה הבאה לא "יתקעו" תמיד במסך הוספה.

## מחזור חודשי אישי (`monthStartDay`, נוסף 2026-09-08)

**החודש כבר לא חייב להתחיל ב-1.** זה שינוי עמוק ונוגע כמעט לכל מקום שמציג "חודש" — קרא את הסעיף הזה במלואו לפני שנוגעים בלוגיקת תאריכים.

- `state.settings.monthStartDay` (1-31, ברירת מחדל 1) — היום שבו "החודש" של המשתמש מתחיל. `state.settings.monthCycleSet` (בוליאני) — האם המשתמש כבר נשאל/אישר את זה (נפרד מהערך עצמו, כי ברירת מחדל 1 יכולה להיות גם "לא נשאל עדיין" וגם "בחר 1 במפורש").
- **`{y,m}` בכל מקום (`ui.hm`/`ui.lm`/`ui.rm`) הוא כבר לא "חודש קלנדרי" — זה "אינדקס מחזור"**. משמעות: מחזור `(y,m)` הוא הטווח שמתחיל ביום `monthStartDay` של חודש קלנדרי `m` בשנה `y` (מוצמד לתאריך תקף אם החודש קצר מדי) ומסתיים יום לפני תחילת מחזור `(y,m+1)`. כשה-`monthStartDay===1` זה בדיוק זהה למה שהיה קודם — **אסור שתהיה נסיגה** בהתנהגות ברירת המחדל.
- **פונקציות הליבה** (ליד `monthTx`): `cycleStartDate(y,m)`, `cycleStartISO(y,m)`, `cycleEndISO(y,m)` (בלעדי), `cycleDayCount(y,m)`, `cycleOf(iso)` (מחזיר `{y,m}` של המחזור שתאריך נתון שייך אליו). **`monthTx(y,m)` עברה משוואת `date.slice(0,7)===ymKey` לטווח תאריכים אמיתי (`>=start && <end`) — כל מקום אחר שסינן תנועות/משמרות לפי "אותו חודש" (`hoursInMonth`, קרן, "שבוע מול שבוע" בדוחות) עבר לאותה שיטה. `ymKey(y,m)` עדיין קיימת ותקינה — היא רק מפתח ייחודי (למשל ל-`goalToast` dedup, לשם קובץ CSV), לא סינון תאריכים, so **אל תשתמשי בה לסינון**, רק ל-`cycleStartISO`/`cycleEndISO`/`cycleOf`.
- `monthLabel(o)` — כש-`monthStartDay<=1` מציגה בדיוק את השם הישן ("אוגוסט 2026"); אחרת מציגה טווח ("15 באוגוסט – 14 בספטמבר") דרך `monthLabelShort`/`dfDM`. אם מוסיפים תצוגת חודש חדשה במקום כלשהו — **תמיד** דרך `monthLabel`/`monthLabelShort`, לא `dfMY.format` ישירות.
- **`ensureRentExpense`/`ensureInsuranceExpense` לא קשורות למחזור הזה בכלל** — הן ממשיכות להשתמש ב-`billingDay` וחודש קלנדרי אמיתי לצורך "יורד פעם בחודש", כי זה מתי שהחיוב האמיתי קורה, לא קשור לאיך שהמשתמש בוחר להסתכל על הדוחות שלו. שתי הגדרות נפרדות ומכוונות.
- **גרפים/מפת חום** (`renderHeatmap`, `drawMonth`) עברו מ-`for(i=1;i<=daysInMonth;i++)` להליכה על התאריכים האמיתיים של המחזור (`cycleStartDate` + `cycleDayCount`), ומציגים את יום-בחודש **האמיתי** של כל תא (לא את המיקום היחסי במחזור) — כי מחזור שחוצה שני חודשים קלנדריים לא יכול פשוט למספר 1..n.
- **אשף חד-פעמי למשתמשים קיימים** (`#monthCycleModal`, `maybeShowMonthCycle()`): מוצג **רק** ל-`wizardDone && !monthCycleSet` — נשרשר אחרי `maybeShowWhatsNew()` (אם היא לא מציגה כלום, קוראת ל-`maybeShowMonthCycle()` ישירות; אם היא כן מציגה, `closeWhatsNew()` קוראת לזה אחריה) כדי שהמודלים לא יתנגשו. **משתמש חדש** מקבל `monthCycleSet=true` מיד ב-`cgMode==='new'` (בדיוק כמו `lastSeenVer`) ולעולם לא רואה את זה. **לא נוגע בשום תנועה** — רק בהגדרה.
- **"6000 בכרטיס היום שלך"**: תנועות שנוצרות אוטומטית ע"י `ensureRentExpense`/`ensureInsuranceExpense` מסומנות `lumpAuto:true`. כרטיס "היום שלך" (`dTx` ב-`renderHome`) **מסנן אותן החוצה** מהחישוב (`!x.lumpAuto`) כדי שסכום ענק חד-פעמי לא יעוות את המאזן היומי — אבל **התנועה עצמה נשארת בשלמותה** ב-`state.tx`, מופיעה מלאה במסך "תנועות" (`renderList`), בדוחות, ב-CSV, ובמפת החום. אם יש `lumpAuto` תנועה ביום הנצפה, מוצגת עליה הערה קטנה ושקופה ב-`#tFoot` ("גם ירד היום X — נספר בחישוב החודשי, לא כאן") — **אל תסירי את ההערה הזו**, זו הדרך שהמשתמש יודע שהכסף לא "נעלם", רק לא נספר בכרטיס היומי.

## תיקון באג RTL/מינוס (`bidiNum`, נוסף 2026-09-08)

**כל מקום שמציג סכום עם סימן (+/−) חייב לעבור דרך `bidiNum(...)`.** הבאג שדווח ("המינוס לא במקום") הוא קלאסי ב-RTL: `fmt()`/`fmtInt()` בעצמן כבר עוטפות את התוצאה שלהן ב-`bidiNum()` (עם תווי בידי-איזולציה יוניקוד LRI/PDI, לא CSS), אבל **כל מקום שמוסיף סימן חיצוני** (`'+'+fmt(v)` / `(x>=0?'+':'−')+fmt(x)`) **חייב לעטוף את כל הביטוי המחובר** ב-`bidiNum(...)` בנפרד, אחרת הסימן שנוסף מבחוץ לא מוגן. **אל תנסי לתקן דברים כאלה עם CSS (`direction`/`unicode-bidi` על `.num`)** — כבר ניסינו וזה שבר את היישור של המספרים הגדולים (הזזה שמאלה לא רצויה); הפתרון הנכון היחיד כאן הוא ברמת המחרוזת (JS), לא ברמת ה-CSS.

## כסף קולי (`parseVoiceCommand`, נוסף 2026-09-08)

- כפתור מיקרופון בטופבר (`#voiceBtn`) — **מוסתר כברירת מחדל**, נחשף רק אם `voiceSupported()` (בדיקת `window.SpeechRecognition||webkitSpeechRecognition`) אמת בזמן העלייה. עובד טוב בכרום/אנדרואיד/סמסונג אינטרנט (כולם מבוססי-Chromium); התמיכה בספארי/אייפון פחות ודאית — ההסתרה האוטומטית כבר מטפלת בזה, **אל תניחי שזה עובד בכל דפדפן**.
- `parseVoiceCommand(text)` — כל לוגיקת הפענוח של המשפט העברי. **קריטי: אסור להשתמש ב-`\b` (word boundary) ברגקסים על טקסט עברי** — `\b` ב-JS מוגדר יחסית ל-`\w` (אותיות לועזיות/ספרות בלבד), אותיות עבריות לא נחשבות `\w`, אז `\b` פשוט לא עובד ליד מילים בעברית (נתפס בפועל: `/ביט\b/` פספס "ביט" בסוף משפט). להשתמש בהתאמת מחרוזת פשוטה (`/ביט/`) במקום.
- זיהוי סכום: הרגקס לוקח את המספר הראשון בטקסט, **אחרי** שמסירים ממנו קודם כל התאמת אחוזים (`X% / X אחוז`) — כדי שאחוז עמלה לא ייתפס בטעות כסכום. סדר החילוץ (קודם %, אחר כך סכום מהטקסט שנשאר) חשוב.
- הזרימה: מקשיב → מזהה → מציג אישור ("זיהיתי: הכנסה · ₪250 · נסיעות · ביט") עם הטקסט הגולמי למטה לשקיפות → "שמירה" (שומר ישירות, בלי לפתוח את מסך ההוספה בכלל — עושה זאת ע"י הגדרת `ui.*` הרלוונטיים ואז `$('#saveTx').click()` פרוגרמטית, כדי לעבור דרך **אותה** לוגיקת שמירה בדיוק [שיאים/יעד/direct-pay/הכל] ולא לשכפל אותה) או "לערוך" (פותח את מסך ההוספה עם השדות ממולאים מראש, כמו `quickAdd`, לבדיקה/שינוי ידני לפני שמירה). **אין תמיכה קולית ל"נסיעה ישירה לסדרן"** — לא נבנה בכוונה (מורכב מדי לזיהוי קולי אמין), רק סוג/סכום/קטגוריה/אמצעי תשלום/אחוז עמלה.
- קיצור מ-`manifest.json`: `?voice=1` פותח את המודל אוטומטית בעליית האפליקציה (ולא רק מהכפתור).
- **נבדק ביסודיות**: לוגיקת הפענוח בלבד (13+ ניסוחים שונים, מ-mock אמיתי של ה-API ב-Playwright, לא re-implementation מבודד) — זיהוי קולי אמיתי (מיקרופון/רשת) לא ניתן לבדיקה ב-headless, לא נבדק ידנית מול מיקרופון אמיתי.

## חדשות רכב (נוסף 2026-09-08, גרסה כנה — לא אוטומטית)

המשתמש ביקש עדכוני מחיר דלק + התרעות הפגנות/חוקים "אוטומטי". **נבדק בפועל (חיפוש אמיתי): אין API ציבורי/רשמי למחיר דלק בישראל** — משרד האנרגיה מפרסם הודעה חודשית (טקסט/פייסבוק), לא feed. בהתאם לכך, מה שנבנה בפועל בכרטיס "חדשות רכב וכביש" (מסך דוחות) הוא **גרסה כנה, לא אוטומטית**: מחיר דלק כמספר קבוע שמתעדכן ידנית (יש הערה מפורשת בתוך הכרטיס שאומרת את זה למשתמש — **אל תסירי את ההערה**), ושני קישורים מהירים אמיתיים ושימושיים (Google Maps לתנועה, אתר משרד התחבורה). **אם המשתמש מבקש שוב אוטומציה/התרעות אמיתיות פה** — זה דורש שרת (Cloud Function בתזמון + Firestore + FCM), בדיוק כמו שהוסבר לו לגבי המפה/הבוט; אל תבני משהו שמתחזה לאוטומטי בלי שזה קיים באמת.

## כללים כשרוצים לשנות

1. שמור על RTL ועברית תקינה.
2. אל תעשה את ה-UI צבעוני/ילדותי.
3. הוסף אייקונים חדשים ל-`sw.js` ב-`ASSETS`.
4. אחרי שינוי משמעותי — הגדל את שמות הקבצים המותאמים (cache name ב-`sw.js`) כדי שמשתמשים מותקנים יקבלו עדכון.
5. שמור על תאימות מובייל (טלפון ראשון).
6. אם אתה מוסיף שדה ל-State — הוסף ברירת מחדל ב-`normState(d)`.

## פריסה

הפרויקט נפרס ל-GitHub Pages. המדריך המלא בקבצים: `guide.html` ו-`README.md`.

הפריסה רצה דרך GitHub Actions (`.github/workflows/deploy.yml`) — `git push` ל-`main` מפעיל דיפלוי אוטומטית. **לוודא שדיפלוי הסתיים**: `gh run list --workflow=deploy.yml`.

## פיצ'ר "הפצת נסיעה" — הוסר לגמרי (2026-09-09)

בעבר היה כאן פיצ'ר מוסתר (feature flag) של הפצת נסיעות לקבוצות וואטסאפ — גרסה ידנית (העתק-הדבק) ואז גם גרסת בוט אוטומטי (Baileys, פרויקט נפרד `wa-bot`). **המשתמש ביקש להסיר את כל העניין לגמרי** כדי שהאפליקציה תישאר נקייה ולא עמוסה. הוסר במלואו: הטאב, כרטיס ההגדרות, כל ה-state (`bcGroups`/`bcHistory`/`bcSession`/`bcSignature`/`botUrl`/`botKey`), כל הפונקציות (`isBroadcastAllowed`, `renderBroadcast`, `applyFeatureFlags`, `bcSigText`, `botReady`/`botBaseUrl`/`botSend`, וכו'), שני המודלים (ניהול קבוצות + טופס קבוצה), ה-`bc-config.js` וה-secret `BROADCAST_ALLOWLIST` מה-workflow (השאירי את שאר ה-workflow — הוא גם מבצע את הדיפלוי עצמו לא רק את זה).

**אם המשתמש מבקש את זה בחזרה אי פעם** — זה נבנה פעמיים בעבר ונמחק פעמיים; לפני שבונים שוב, לזכור: (1) גרסה אוטומטית-שולחת-לבד מפרה תנאי שימוש של וואטסאפ ומסכנת חסימה — המשתמש כבר קיבל את ההסבר הזה וסירב פעם אחת לפני שחזר וביקש בכל זאת; (2) הבוט (אם ירצה שוב) רץ על מחשב אישי, לא בענן; (3) בפעם האחרונה זה היה feature flag מוסתר לקוד סנכרון ספציפי — לא כלל את כל המשתמשים.

## עדכון 2026-09-09: "עדכון פסיכי" — נעילה, שיתוף משפחה, הסיפור שלי, מצב רכב, מפת חום שני-מימדית, מנוע יומי (v10)

המשתמש ביקש סבב גדול של רעיונות "וואו" (קסם אוטומטי + חוויה חושית, בלי עלות, בלי כאב ראש רב) ואז ביקש לבנות את כולם בבת אחת "ברמה הכי גבוהה... שלא יראה מפוצץ". **הוחלט במפורש לא לבנות הכול פשוטו כמשמעו** — שני רעיונות הוצאו מהיקף הבנייה מסיבות מפורשות שהוסברו למשתמש, ולא נבנו:
- **סריקת קבלה במצלמה (OCR)** — דורש ספרייה חיצונית (כמו Tesseract.js), שובר את העיקרון של "קובץ יחיד, בלי build step"; דיוק לא אמין על קבלות מצולמות בעברית.
- **זיהוי אוטומטי של תחילת/סוף נסיעה לפי תנועת הטלפון** — הרשאות רקע מורכבות, סוללה, הרבה false positives (נוסע ברכב כנוסע ולא כנהג). הוחלט לחכות להחלטה משותפת עם המשתמש אם ירצה בכך.
גם "טיקר חי בהתראות" (התראה שמתעדכנת לאורך היום) ו"מסע בזמן אחורה" (מסך memory-lane נפרד) לא נבנו — נשקלו כמיותרים/מיותר-קרובים לפיצ'רים קיימים (חצי ניווט בין ימים כבר קיים) ומסוכנים ל"עומס התראות" בדיוק מה שהמשתמש לא רצה.

מה שכן נבנה, בקצרה (כל הפיצ'רים הבאים חינמיים לגמרי — משתמשים רק ב-Firebase שכבר קיים, ב-API-ים מובנים בדפדפן, בלי שרת חדש, בלי כרטיס אשראי):

### מנוע יומי (בוקר-ערב-שבועי) — `checkMorning()`, `checkWeekly()`, `checkOverwork()`
- `state.settings.morningOn`/`morningTime` (ברירת מחדל: כבוי, 08:00) — התראה מקומית (`Notification`, לא push אמיתי משרת) עם: נטו אתמול, מרחק ליעד, רצף, ו"נבואה" (`weekdaySuccessChance(wd,avg)` — % מהימים ההיסטוריים באותו יום בשבוע שעברו את הממוצע היומי הכללי, `avgDailyNet()`).
- `checkWeekly()` — כל יום ראשון (אחרי שעת הבוקר טוב), משווה שבוע שעבר מול השבוע שלפניו, גייטד ב-`state.meta.weekShown` (תאריך).
- `checkOverwork()` — משווה שעות השבוע הנוכחי (`weeklyHoursEndingToday`) מול ממוצע 6 השבועות הקודמים; מתריע רק אם יש **לפחות 3 שבועות עבר עם נתונים** (אחרת שקט, לא ממציא נתון) והשבוע הנוכחי גבוה ב-30%+. גייטד ב-`state.meta.overworkWeek` (מפתח = תחילת השבוע, `weekStartISO`).
- **כל אלה מקומיות בלבד** (לא FCM/push אמיתי) — עובדות רק כשהאפליקציה פתוחה/ברקע קרוב, בדיוק כמו התזכורת הישנה `checkNotify()`. שדרוג לpush אמיתי דורש Firebase Blaze (כרטיס אשראי) — הוסבר למשתמש והוא בחר בגרסה החינמית.

### מנוע אבני דרך + חגיגות — `checkMilestones()`, `celebrate()`
- `checkMilestones(tx,d)` נקרא מ-`saveTx` אחרי כל שמירה, בודק (בסדר עדיפות): סכום מצטבר עגול (1,000 עד מיליון, ב-`state.meta.milestones['life_'+th]`), רצף ימים (7/14/30/60/100, `'streak_'+sd`), שיא אישי ליום בשבוע הספציפי (`'wd_'+wd`, נשמר כתאריך כדי לאפשר עדכון). **כל אבן דרך נחגגת פעם אחת בלבד** — הדגלים ב-`state.meta.milestones` לא מתאפסים לעולם.
- `celebrate(emoji,title,sub)` — הכליל את המודל הישן `#goalCelebrate` (שהיה רק ליעד חודשי) לשימוש כללי: גשם מטבעות אמיתי (`spawnCoinRain()`, DOM אמוג'י מונפשים ב-CSS, לא canvas) + צליל "צלצול" עולה (`playCelebrationSound()`, Web Audio, 4 טונים). בשימוש כרגע ב-3 מקומות ב-`saveTx`: הגעה ליעד (עדיפות ראשונה), אבן דרך (שנייה), שיא יומי חדש (שלישית) — **רק אחד מוצג בכל שמירה**, לא נערמים.

### קול — `speak()`, כפתור השמעת יום, ברכת בוקר
- `speak(text)` — עטיפה סביב `SpeechSynthesis` המובנה בדפדפן (`he-IL`), מכבד את `state.settings.soundOn`. **דורש ז'סטורת משתמש בהרבה דפדפנים (בעיקר ספארי/אייפון)** — קריאה אוטומטית מה-boot (ברכת הבוקר) עלולה להיכשל בשקט (try/catch), זה בסדר ומכוון, לא לתקן ל"תמיד עובד".
- כפתור רמקול בכרטיס "היום שלך" (`#speakDayBtn`, מוצג רק כשצופים בהיום הנוכחי) קורא ל-`daySummaryCache` (מתעדכן בכל `renderHome()`) ומקריא הכנסה/הוצאה/רכב/מאזן.
- ברכת בוקר: בעליית האפליקציה, פעם ביום (`state.meta.morningGreeted`), אחרי 1.2 שנ' השהיה.

### נעילת פרטיות — `#lockGate`
- **לא אבטחה אמיתית** — קוד 4 ספרות נשמר גלוי ב-`state.meta.lockPin` (חלק מה-state שמסונכרן ל-Firestore ולגיבוי JSON). זו "מגן חברתי" בלבד (מישהו שמושיט יד לטלפון), לא הגנה מפני מי שיש לו גישה לנתונים בכל מקרה. **אם המשתמש ירצה אבטחה אמיתית בעתיד — זה דורש ארכיטקטורה אחרת** (הצפנה בצד לקוח לפני שמירה, לא רק שכבת UI).
- נבדק חוזר: הגדרה → נעילה בטעינה חוזרת → קוד שגוי מציג שגיאה → קוד נכון פותח ושומר ב-`sessionStorage`.

### שיתוף משפחה — `pushPublicShare()`, `share.html`
- קולקציית Firestore נפרדת `publicShare/{shareId}` (לא `kupaDriverAccounts`) — מכילה **רק** `{name,net,hours,streak,updatedAt}` של היום הנוכחי, בלי שום פירוט הכנסות/הוצאות/יעדים. `shareId` נוצר פעם אחת (`state.meta.shareId`), לא הקוד האישי עצמו.
- `share.html` — עמוד ציבורי נפרד לגמרי (לא חלק מה-PWA המותקן, לא ב-`sw.js` ASSETS), קורא ל-Firestore ישירות דרך אותו `firebaseConfig`, `onSnapshot` בזמן אמת. אין בו סנכרון, אין התחברות, אין קוד אישי — קישור-צפייה בלבד.
- **חוקי Firestore פתוחים לגמרי** (`allow read, write: if true`) כמו שאר הפרויקט — אם אי פעם מהדקים את החוקים, `publicShare` צריך `allow read: if true; allow write: if request.resource.data.keys().hasOnly(['name','net','hours','streak','updatedAt'])` בערך, לא לפתוח כתיבה חופשית.

### מצב תצוגה לרכב — `#carModeOverlay`, `updateCarMode()`
- מגיב ל-`matchMedia('(orientation: landscape)')` — מוצג אוטומטית כשהטלפון לרוחב **וגם** `state.settings.carModeOn` (ברירת מחדל: מופעל) **וגם** `ui.screen==='home'`. נסגר בנגיעה או בחזרה לאורך. **לא חוסם מסכים אחרים בלרוחב** (דוחות/רשימה) — נבדק בכוונה, כדי לא להפריע לצפייה בגרפים לרוחב.

### מפת חום שני-מימדית — `renderWeekHourHeat()`
- שונה מ"מפת חום — איך היה כל יום" הקיימת (זו לפי תאריך בלוח שנה) — זו לפי **יום בשבוע × שעה ביום** (`t.ts` של כל תנועת הכנסה, מחולק ל-6 בלוקים של 4 שעות). מוסתרת לגמרי (`display:none`) אם אין עדיין נתוני `ts` מספיקים (משתמש חדש).

### הסיפור שלי — `openStory()`, `#storyModal`
- מסך "סטורי" מלא (כמו Instagram/Spotify Wrapped) שנבנה מההיסטוריה המלאה של המשתמש — לא מוגבל לשנה קלנדרית (למרות השם "שנתי" שעלה בשיחה, בפועל זה "כל הזמנים" כי משתמש בודד לא בהכרח צבר שנה שלמה של נתונים). ניווט בנגיעה בצדדים (ימין=הבא, שמאל=הקודם, מוסבר בקוד כי `left`/`right` ב-CSS הם פיזיים ולא לוגיים, אז זה עובד נכון גם ב-RTL).

**גרסה: APP_VER=10, `sw.js` CACHE=kupa-v3-14.**

## עדכון 2026-09-09 (המשך אותו יום): טאב "יומן", זיהוי קולי אמין יותר, אנימציות מסך (v11)

המשתמש אמר במפורש שהעדכונים הקודמים לא הרגישו "מיוחדים" ונתן דוגמה קונקרטית: מישהו מעביר לו כסף בבנק והוא צריך לזכור להעביר את זה הלאה ב-Bit — וזה נשכח כי "אף אחד לא עושה סגירת יום". זה הוביל לטאב שלם חדש, לא עוד תוספת קטנה.

### טאב חדש "יומן" (`#s-journal`, `data-go="journal"`)
- **`state.journal`** — מערך פריטים חדש (לא `state.tx`!): `{id,text,amount,tag,done,ts,doneTs}`. `tag:'passthrough'` מסמן "כסף עובר, לא שלי" (בדיוק המקרה של Bit). **קריטי: משימות יומן לעולם לא נכנסות ל-`state.tx` ולא משפיעות על שום חישוב כספי** — זה בכוונה, כדי שלא תקרה טעות אמיתית של לספור "כסף עובר" כהכנסה.
- **הבחנה בין תנועה למשימה בקלט הקולי** (`parseVoiceCommand`): אם הטקסט מתחיל במילות טריגר (`תזכיר לי`, `לא לשכוח`, `צריך ל`, `משימה`) — זו משימה, לא תנועה, גם אם יש בה מספר. הפונקציה מחזירה `{isTask:true,...}` ומדלגת על כל לוגיקת ההכנסה/הוצאה. **אם מוסיפים מילות טריגר נוספות בעתיד, לוודא שהן לא חופפות לניסוחי תנועה רגילים** (נבדק מול כל 13+ הניסוחים הישנים — אף אחד לא הופעל בטעות כמשימה).
- זיהוי אוטומטי (היוריסטי, לא מושלם) של "כסף עובר": מילות מפתח `להעביר/העברה` + `ביט/מישהו/לו/לה` → `tag:'passthrough'`. המשתמש תמיד יכול לתקן ידנית ב-checkbox במודל האישור (`#voiceTagPassthrough`) או פשוט למחוק ולהוסיף מחדש.
- **הוספה גם בלי קול** — `#journalAddInput`/`#journalAddBtn` בראש המסך, אותה היוריסטיקה בדיוק. חשוב: קול הוא תוספת, לא תלות — היומן חייב לעבוד גם למי שהקולי לא עובד לו (למשל בעיית Samsung למטה).
- **ציר זמן יומי** (`renderJournalTimeline`) — ממזג היום הנוכחי בלבד (לא היסטוריה): תנועות כסף (`state.tx` בתאריך היום, לא כולל `lumpAuto`), משימות שנוצרו היום, ומשימות שהושלמו היום — הכל ממוין לפי זמן אמיתי (`ts`), לא לפי סוג.
- **תג על הטאב עצמו** (`#journalBadge`, `updateJournalBadge()`) — מספר המשימות הפתוחות, מתעדכן בכל שינוי ליומן ובעליית האפליקציה.
- **כפתור "השמעת היום"** (`#journalSpeakBtn`) — קורא ל-`speak()` הקיים, מקריא הכנסות/הוצאות/מאזן + כל המשימות הפתוחות בשם. מיועד לשימוש ב-1 בלילה כשלא בא לקרוא מסך.

### שעה חכמה לתזכורת סגירת יום (`state.settings.smartCloseOn`, ברירת מחדל: מופעל)
- **הבעיה שהמשתמש הצביע עליה**: שעה קבועה (21:30) לא רלוונטית כי אנשים מסיימים לרשום בשעות שונות כל יום. הפתרון: `computeAdaptiveCloseHour()` בודק, לכל יום בהיסטוריה, מה השעה **המאוחרת ביותר** שבה נרשמה תנועה, ולוקח את ה**חציון** (median) של כל הימים — לא ממוצע, כדי שיום חריג אחד (נסיעה עד 3 בלילה) לא יזיז את השעה לכולם.
- **דורש לפחות 5 ימים של היסטוריה** (`vals.length<5`) — לפני זה, `effectiveCloseHourMinute()` נופל בחזרה ל-`state.settings.remindTime` הידני. **אל תורידי את הסף הזה נמוך מדי** — עם פחות מ-5 נקודות מידע החציון לא אומר כלום.
- `checkNotify()` ו-`checkOverwork()`/`checkWeekly()` (שממשיכים להשתמש ב-`morningTime` הנפרד, לא ב-`smartCloseOn` — זה **רק** לתזכורת הערב) עודכנו לקרוא ל-`effectiveCloseHourMinute()` במקום parse ישיר של `remindTime`.
- UI: מתג "שעה חכמה" מסתיר את שורת "שעת התזכורת" הידנית (`#remindTimeRow`) כי היא לא רלוונטית כשהמתג דלוק — **לא למחוק את הערך השמור** ב-`remindTime`, רק להסתיר, כדי שהמשתמש יוכל לחזור לשעה קבועה בלי להזין מחדש.
- רשת ביטחון: אם משימה נשארת פתוחה מעל 12 שעות, `checkMorning()` מוסיף שורה "משהו נשאר פתוח ביומן מאתמול" — כך שגם אם התזכורת החכמה בערב לא נתפסה, יש הזדמנות שנייה בבוקר.

### תיקון אמינות קולי (Samsung Internet ודומיו)
המשתמש דיווח שהקולי "לא עובד בסמסונג". **הבדיקה בפועל**: Samsung Internet (הדפדפן שמגיע כברירת מחדל בהרבה מכשירי Samsung, נפרד מ-Chrome) **לרוב לא תומך כלל ב-`SpeechRecognition`/`webkitSpeechRecognition`**, או תומך חלקית עם שגיאות `service-not-allowed`. **זו מגבלת דפדפן אמיתית, אין דרך לעקוף אותה מקוד צד-לקוח** — אי אפשר "לתקן" תמיכה שלא קיימת בדפדפן.
- מה שכן נבנה: `isSamsungInternet()` (בדיקת `navigator.userAgent` על `SamsungBrowser`) — כשה-API לא קיים **ודפדפן סמסונג זוהה**, `showVoiceFail` נותן הודעה ספציפית ומעשית: "תפתח את קופת הנהג דרך כרום" (כרום כמעט תמיד מותקן על מכשירי סמסונג ותומך מצוין בזיהוי קולי).
- הורחבו קודי השגיאה המטופלים (`onerror`): נוסף `audio-capture`, `service-not-allowed`, והודעות קיימות שופרו להיות פעולתיות יותר (למשל "אפשר גישה למיקרופון **ונסה שוב**" במקום רק לתאר את הבעיה).
- נוסף `onend` guard — אם ה-recognition נגמר בלי שום `onresult` (קורה בדפדפנים בעייתיים בלי שום שגיאה מפורשת), עכשיו מוצגת הודעת כישלון במקום שהמודל פשוט "יתקע" על מסך ההקשבה בלי הסבר.
- **החלטה מכוונת: היומן לא תלוי בקולי** (ר' מעלה, `#journalAddInput`) — כדי שהתלונה של "לא עובד בסמסונג" לא תחסום את כל הפיצ'ר החדש, רק את הנוחות הקולית שלו.

### אנימציות מסך כלליות
נוספה `screenIn` (fade + slight scale, `.screen.active`) ו-`cardIn` (fade + slide up, `.card`) — משחקות בכל מעבר טאב ובכל פעם שכרטיס נכנס ל-DOM מחדש (למשל מעבר בין מסכים, לא render מחדש של תוכן קיים). שתיהן מכבדות `prefers-reduced-motion`. **זו תוספת רוחבית מכוונת ומצומצמת** (לא "עברתי על כל האפליקציה") — נבחרה כי היא הכי נראית-לעין בלי לגעת בעשרות מקומות שכבר יש להם אנימציות ייעודיות (goal ring, coin rain, hero numbers, וכו').

**גרסה: APP_VER=11, `sw.js` CACHE=kupa-v3-15.**

## עדכון 2026-09-09 (המשך): תיקון סכנת איפוס נתונים אמיתית + באג מודל "מה חדש" שגדל בלי סוף (v12)

המשתמש דיווח שהכל "התאפס" לו, ושבמודל "מה חדש" הוא יכול לגלול אבל לא להגיע לכפתור "בסדר". שני באגים אמיתיים, לא תלונות סתמיות:

### באג ותיק שנמצא: `cloudPush()` דרס את הענן בלי שום בדיקה
`cloudPush()` עשה `.set()` מלא על מסמך ה-Firestore בכל `save()`, בלי שום השוואה למה שכבר שם. **הסיכון האמיתי**: אם ה-`localStorage` במכשיר מתנקה (קורה בפועל באייפון — ספארי מוחק נתוני אתרים שלא נפתחו 7 ימים, גם לפעמים ל-PWA מותקן תחת לחץ אחסון) אבל `syncCode` בכל זאת נשאר (למשל ניקוי חלקי, או תרחיש אחר שמאפס רק את `state.tx`) — ה-boot היה טוען state ריק, וכל `save()` ראשון (אפילו טריוויאלי כמו הפעלת/כיבוי מתג) היה **דורס את הגיבוי האמיתי בענן בנתונים ריקים, בלי שום התראה**. זו הייתה סכנת איבוד נתונים אמיתית ולא תיאורטית.

**התיקון** (`firstPushChecked`, ב-`cloudPush()`): פעם אחת בלבד לכל טעינת עמוד — אם ה-state המקומי נראה חשוד (`state.tx.length===0&&state.shifts.length===0`) **לפני** ש-`cloudPush` נקרא בפעם הראשונה — במקום לדרוס, קוראים קודם מה-Firestore (`ref.get()`); אם שם באמת יש נתונים (`tx.length>0` או `shifts.length>0`), **משחזרים אותם למקומי** (`state=normState(remote)`) במקום לדרוס, ומראים טוסט "שוחזרו הנתונים שלך מהענן אוטומטית ✓". רק אם גם בענן ריק — ממשיכים בדחיפה הרגילה. אחרי הבדיקה החד-פעמית הזו (`firstPushChecked=true`), כל השאר ממשיך רגיל — **כדי שכפתור "מחיקת כל הנתונים" הקיים (`#eraseBtn`) ימשיך לעבוד כרגיל ולא "יינצל" בטעות בחזרה על ידי ההגנה החדשה**. נבדק בשני התרחישים (Playwright, מול Firestore אמיתי): (1) state מקומי מרוקן בכוונה עם syncCode שרוד → שוחזר אוטומטית, הענן לא נדרס; (2) מחיקה מכוונת דרך הכפתור אחרי שהאפליקציה כבר רצה → מתפשטת רגיל לענן. **אל תסירי את `firstPushChecked` בלי לשמור על שתי ההתנהגויות האלה יחד.**

### באג: `WHATS_NEW` גדל בלי סוף במקום להתאפס כל גרסה
בכל bump גרסה (v9→v10→v11) התווספו פריטים ל-`WHATS_NEW` **בלי להסיר את הישנים** — הגיע ל-15 שורות, מה שהפך את מודל "מה חדש" לגבוה מהמסך, עם כפתור "הבנתי" לא נגיש (אין overflow על `.modal-card`). **כלל חדש: כל bump גרסה חייב להחליף את `WHATS_NEW` לגמרי (2–5 פריטים של השינויים החדשים בלבד), לא להוסיף אליו.**
- תוקן גם ברמת ה-CSS כרשת ביטחון: `.modal-card` קיבל `max-height:85dvh;overflow-y:auto` — **כל** מודל באפליקציה (לא רק "מה חדש") עכשיו מוגן מובנה מתוכן ארוך מדי שדוחף כפתורים מחוץ למסך, גם אם ישכחו את הכלל למעלה בעתיד.

### תלונה על "הטאבים למטה עולים עם הגלילה"
תוקן ברמת ניסיון-הכי-בטוח: נוסף `translateZ(0)` + `will-change:transform` ל-`.tabbar` (טכניקת compositing-layer סטנדרטית שמפחיתה ריצוד/קפיצה של אלמנטים `position:fixed` בגלילה על ספארי מובייל). **לא אומת על מכשיר אמיתי** — זו תופעה ידועה של דפדפני מובייל (בעיקר ספארי) הקשורה לאנימציית כיווץ/הרחבה של ה-address bar, לא ניתנת לשחזור מלא בסביבת בדיקה headless. אם המשתמש מדווח שזה עדיין קורה, השלב הבא (יותר פולשני, לא בוצע כאן כי לא נבדק) הוא לנעול גלילה של `body` ולהפוך מיכל פנימי ל-scroll container היחיד — שינוי ארכיטקטוני אמיתי, לא לבצע בלי לשאול קודם ובלי גישה למכשיר בדיקה אמיתי.

**גרסה: APP_VER=12, `sw.js` CACHE=kupa-v3-16.**

## עדכון 2026-09-09 (המשך): הבאג האמיתי שגרם לאובדן נתונים בפועל — נמצא, שוחזר, תוקן (v13)

המשתמש דיווח שתנועות אמיתיות מ-6–9 בספטמבר נעלמו. **זה לא היה בלבול תצוגה** (כמו מעבר לחודש חדש שטיפלנו בו קודם) — בדיקה ישירה מול Firestore האמיתי (עם קוד המשתמש `0533abcA`, דרך REST API של Firestore בלי שום credentials מיוחדים — הפרויקט משתמש בחוקי אבטחה פתוחים `allow read, write: if true`) אישרה: 93 תנועות בסך הכל, מ-1 באוגוסט ועד 6 בספטמבר בלבד — **שום תנועה מ-7–9 בספטמבר לא הייתה שם בפועל**.

### הבאג האמיתי (נמצא ותוקן): מרוץ בין `cloudPush` ל-`onSnapshot`
`startCloudSync()`'s `onSnapshot` היה מקבל כל שינוי במסמך הענן ומחליט אם "לקבל" אותו לפי השוואת `remote.updatedAt` (טיימסטמפ קליינט מוטבע בתוך ה-JSON) מול `state.updatedAt` המקומי. **הבעיה**: אם המשתמש הוסיף תנועה מקומית (state עודכן, `save()` נקרא, `localStorage` נכתב מיד — אבל ה-push לענן ממתין 1200ms debounce) — וב**חלון הזמן הזה בדיוק** הגיע `onSnapshot` עם מסמך אחר (מסיבה כלשהי: התחברות מחדש, listener שמתחבר, טעינה ראשונית של snapshot, או אפילו מכשיר/טאב שני עם שעון קליינט שמקדים מעט) — הקוד היה **מחליף את כל ה-`state` בזיכרון** (`state=normState(remote)`) בלי שום הגנה, **מוחק בשקט** את התנועה שזה עתה נוספה מקומית ועדיין לא הספיקה להישלח. זה בדיוק ההסבר: הוא הוסיף תנועות אמיתיות ב-6–9 בספטמבר, וברגע שכזה נכנס snapshot מתחרה (סביר להניח מפעולת רקע כלשהי, אולי מקושרת לעדכון v11/v12 שהתפרס באותו יום ורענן את החיבור), התנועות נדרסו בזיכרון לפני שהספיקו להישלח לענן — ואז ה-state הזה (בלי התנועות) **נשמר בעצמו** ל-localStorage וגם ל-Firestore, מוחק אותן סופית משני המקומות.

**התיקון**: משתנה `pendingLocalPush` (ליד `firstPushChecked`) — נקבע ל-`true` ברגע ש-`cloudPush()` נקרא (לפני ה-debounce), ונשאר `true` עד שה-push בפועל מסתיים (הצלחה או כישלון). `onSnapshot` בודק את זה **ראשון, לפני הכל**: `if(pendingLocalPush)return;` — כל עוד יש עריכה מקומית "בדרך" לענן, שום snapshot נכנס לא יכול לדרוס אותה, לא משנה מה ה-`updatedAt` שלו. **נבדק ישירות** (Playwright, מדמה מרוץ אמיתי: הוספת תנועה מקומית ואז הזרקת snapshot מתחרה עם `updatedAt` "מהעתיד" תוך 300ms) — התנועה המקומית שורדת ומגיעה בסוף לענן, בלי קשר לתזמון.

### באג נוסף שנמצא תוך כדי הבדיקה: עדכון ממכשיר אחר לא נשמר ל-localStorage
כש-`onSnapshot` כן מקבל snapshot לגיטימי ממכשיר אחר, הקוד עדכן את ה-state בזיכרון ורנדר מחדש — **אבל מעולם לא שמר את זה ל-`localStorage`**. אם המשתמש היה סוגר את האפליקציה בלי לעשות שום עריכה מקומית משלו אחרי הסנכרון, הפתיחה הבאה הייתה טוענת שוב את הגיבוי המקומי **הישן**, "מאבדת" בחזרה את מה שהגיע ממכשיר אחר. תוקן: `onSnapshot` כותב עכשיו גם ל-`localStorage` ישירות (לא קורא ל-`save()` המלא כדי לא ליצור לולאת push-פינג-פונג חוזרת לענן על מה שרק התקבל ממנו).

### שחזור הנתונים שאבדו למשתמש
המשתמש שלח גיבוי JSON שהוציא ממכשיר שני (`#backupBtn`) שעדיין החזיק את התנועות האבודות מקומית. **לעולם אל תדרסי `state` בענן בלי מיזוג מפורש לפי `id` של כל תנועה** — גם בשחזור ידני כזה, יש למזג (union לפי `tx[].id`) בין מה שכבר בענן למה שבגיבוי, לא סתם להחליף, כדי לא לאבד שום דבר שהצטבר משם.

**גרסה: APP_VER=13, `sw.js` CACHE=kupa-v3-17.**

## עדכון 2026-09-09 (המשך): גיבוי יומי אוטומטי בענן — רשת ביטחון אמיתית (לא v-bump, שינוי שקט)

אחרי שחזור התנועות שאבדו (ראה למעלה), המשתמש ביקש במפורש "תדאג שזה לא יקרה בחיים יותר". התיקון ל-race condition כבר סוגר את הסיבה הידועה — אבל "לעולם לא" מחייב גם רשת ביטחון בלתי תלויה בכך שמצאנו את כל הבאגים. **Firestore בפרויקט הזה לא מוגדר עם Point-in-Time Recovery** (פיצ'ר בתשלום שדורש הפעלה מראש) — כלומר עד עכשיו, אם המסמך החי היה נהרס מכל סיבה שהיא (באג עתידי, טעות אנוש, מה שלא יהיה), **לא הייתה שום דרך לשחזר** מלבד מה שבמקרה שרד על מכשיר שני (בדיוק המזל שהיה למשתמש הפעם).

**מה שנוסף**: `pushDailyBackup()` — בכל push מוצלח לענן (בתוך `doPush()`), נכתב גם snapshot מלא לקולקציה נפרדת `dailyBackups`, במסמך בשם `{syncCode}_{YYYY-MM-DD}`. כל יום מקבל מסמך נפרד משלו (לא נדרס בין ימים) — כך שגם אם המסמך הראשי `kupaDriverAccounts/{code}` נהרס באמצע היום, **הגיבוי של אתמול (ושל כל יום קודם) נשאר נקי ובלתי נגיע**, בלי תלות בשום דבר שקורה למסמך החי. עלות: כמעט אפס (מסמך JSON קטן ליום, בתוך המכסה החינמית של Firestore בנקל).

**איך משחזרים ממנו בפועל** (לפעם הבאה): שאילתת REST פשוטה בלי שום credentials מיוחדים (כמו שנעשה כאן, כי חוקי האבטחה פתוחים):
```
GET https://firestore.googleapis.com/v1/projects/sodg-272b7/databases/(default)/documents/dailyBackups/{syncCode}_{YYYY-MM-DD}
```
זה מחזיר את ה-state המלא (`fields.data.stringValue`, JSON) כפי שהיה בסוף היום ההוא. **לשחזור, תמיד למזג לפי `tx[].id` לתוך המסמך החי הנוכחי (`kupaDriverAccounts/{code}`) — לא לדרוס אותו** (בדיוק כמו שנעשה עם הגיבוי הידני של המשתמש).

**אין עדיין ניקוי אוטומטי של מסמכים ישנים** — כל יום נשאר לצמיתות. בקנה מידה של אפליקציה אישית זה זניח (כמה עשרות/מאות KB לשנה), אבל אם זה אי פעם רלוונטי (הרבה משתמשים), אפשר להוסיף מחיקה של מסמכים מעל גיל מסוים (Cloud Function מתוזמנת) — לא בוצע כי לא נדרש בקנה המידה הנוכחי.

## עדכון 2026-09-09 (המשך): שינוי חוויית הליבה — לא עוד מסך, אלא מה שנוגעים בו כל יום (v14)

המשתמש חזר בפעם השנייה על "כל שינוי לא מרגיש כמו כלום" — הבנה חשובה: רוב מה שנבנה קודם (יומן, הסיפור שלי, מפות חום) יושב במסכים משניים שהוא לא פותח כל יום. הפעם כל השינויים ב**מסך הבית ומסך ההוספה** — המקומות שנוגעים בהם 20 פעם ביום.

- **`updateGlobalProgress()`** — פס דק (`#globalProgress`, `position:fixed;top:0`) שרץ מעל הכל, בכל מסך, תמיד. `%` = נטו החודש חלקי היעד. נקרא מ-`go()` (כל מעבר מסך) וב-boot — **קריטי: אם מוסיפים מסך חדש עם `go(id)`, הפס מתעדכן אוטומטית, אין צורך לגעת בו**.
- **מצב-רוח ויזואלי על ה-Hero** — `document.documentElement.dataset.mood` (`'great'`/`'low'`/נמחק ל-`'ok'`) נקבע ב-`renderHome()` **רק כשצופים בהיום הנוכחי** (`td===todayISO()`), מאותו חישוב בדיוק כמו `#moodFace` הקיים (`bal` מול `avgDailyNet()`). CSS: `html[data-mood="great"/"low"] .hero::before` — **מוצב אחרי** כללי ה-`data-daypart` הקיימים בגיליון הסגנונות כדי לנצח אותם בעדיפות שווה (מצב-רוח חשוב יותר משעת היום). **לא נגעתי ב-body/רקע כללי** — בכוונה, כדי לא לסכן את כל שאר העיצוב; הטון מוגבל לכרטיס ה-Hero בלבד.
- **קטגוריה מתקפלת בהוספה** (`#catSummary`/`#catGrid.collapsed`) — **רק בהכנסה**, לא בהוצאה. חשוב: **הוצאה נשארת עם הרשת פתוחה תמיד** — נבדק בפועל שקיפול ברירת מחדל גם בהוצאה שובר את הזרימה (הקטגוריה משתנה כל פעם, לא כמו "נסיעות" שחוזר על עצמו כמעט תמיד). `buildCats()` קובע `collapsed`/`display` של `#catSummary` לפי `ui.addType==='income'` — **אם מוסיפים סוג שלישי ל-`ui.addType` אי פעם, לעדכן את התנאי הזה**.
- **`.hero-net.hero-flash`** — אנימציית קפיצה+זוהר על `#netNum` עצמו (לא מודל נפרד), מופעלת ב-`popEl($('#netNum'),'hero-flash')` בכל שמירת תנועה מוצלחת, **לפני** הבדיקות של celebrate()/milestone/record — כך שגם "יום רגיל" מקבל תגובה קטנה על המספר עצמו, לא רק רגעים גדולים שמקבלים את המודל המלא.
- **ספירה לאחור לימי עבודה ביעד** (`renderGoal`) — `workdaysLeft=Math.ceil((g-net)/avgDailyNet())`, מוצג רק כש-`avgDailyNet()>1` (יש מספיק היסטוריה) וכש-`!reached`. חשבון פשוט, אין AI.
- **`#healthOverlay`** — מסך מלא ודרמטי (לא רק Notification) שמופיע כש-`checkOverwork()` מזהה שבוע חריג — נוסף ל-Notification הקיים, לא במקומו. נבדק עם 6 שבועות עבר ~30 שעות מול שבוע נוכחי ~54 שעות (מעל סף 1.3x) — מופיע כמצופה, נסגר ב-`#healthOk`.
- **`#bootLine`** — שורה אחת בתוך `#bootSplash` עצמו, מחושבת **סינכרונית מיד אחרי `state=load()`** (לפני שכל שאר האפליקציה עולה) מתוך `localStorage` בלבד — "היום כבר עשית X" אם יש הכנסה היום, אחרת "בוא נתחיל יום טוב". **חייב להישאר סינכרוני** — אסור לחכות לרשת/Firestore כאן, זה בדיוק הרגע שאמור להיות הכי מהיר באפליקציה.

**גרסה: APP_VER=14, `sw.js` CACHE=kupa-v3-20.**

## עדכון 2026-09-09 (המשך): 10 אנימציות — בכוונה בלי הכרזה (v14 ממשיך, לא v15)

המשתמש דחה לגמרי כיוון של קנבס עיצוב עם 4 קונספטים דרמטיים (ניאון/זהב/גרדיאנט/HUD) שהוצג לו — **הוא לא רוצה לגעת בעיצוב הקיים בכלל**, רק תנועה/אנימציות בתוכו. גם ביקש במפורש **שזה יהיה הפתעה, לא הכרזה** — לכן `APP_VER` **לא** עלה (נשאר 14) ו-`WHATS_NEW` לא נגע — רק `sw.js` CACHE עלה (kupa-v3-21) כדי שהקוד בפועל יתעדכן במכשירים מותקנים בלי לקפוץ מודל "מה חדש". **אם מוסיפים עוד עדכון "הפתעה" בעתיד — אותו עיקרון: לעדכן cache בלי לגעת ב-APP_VER/WHATS_NEW, אלא אם המשתמש מבקש אחרת.**

10 אנימציות נוספו, כולן מכבדות `prefers-reduced-motion`:

1. **ריפל** — `pointerdown` גלובלי על `.card,.mini-card` (מדלג אם הלחיצה על button/input/label/a/textarea בפנים, כדי לא לשכפל משוב על אלמנטים שכבר יש להם אנימציה משלהם). דורש `.card`/`.mini-card` עם `position:relative` (נוסף).
2. **מטבע עף מה-FAB לנטו** — `flyCoinToHero()`. **קריטי**: משתמש ב-`.tab-fab` (בטאבבר, תמיד ב-DOM) לא באלמנט במסך ה-add (שנעלם כש-`go('home')` מתבצע) — קורא ל-`getBoundingClientRect()` על שניהם **אחרי** ש-`go('home')` כבר רץ, כדי ששני הרכיבים "קיימים" בו-זמנית.
3. **טבעת יעד מצטיירת מ-0** — `goalRingBooted` (משתנה מודול, לא ב-state) גורם לזה לקרות **פעם אחת בלבד לכל טעינת דף**, לא בכל render. double-`requestAnimationFrame` מבטיח שהדפדפן יצייר את מצב ה-0% לפני שהוא עובר לאחוז האמיתי (אחרת אין "מ" לאנימציה לעבור).
4. **פעימת רצף** — CSS טהור על `.streak-chip`, שם ורק כשמוצג (streak>=2).
5. **רעד על סכום לא תקין** — גם בשמירה עם 0 וגם כשמגיעים למקסימום הספרות בהקלדה (`#pad` handler בודק אם `ui.amt` לא השתנה אחרי `padStep`).
6. **מספרים "מתהפכים" (odometer)** — `renderHeroOdometer()`, **מוגבל ל-`#netNum` בלבד**, לא לכל שאר הקריאות ל-`animateNum` (hInc/hCom/hExp/וכו' ממשיכים לספור חלק כרגיל) — כדי לצמצם את שטח הסיכון. מפרק את המחרוזת המפורמטת לתווים, עוטף רק ספרות ב-`<span class="odo-digit">`, שאר התווים (₪, פסיק, מינוס, סימני bidi) נשארים כטקסט רגיל.
7. **"נשימה" של המספר הגדול** — `.hero-net{animation:heroBreathe 4s infinite}` תמידי. **תלות קריטית עם #2**: `.hero-net.hero-flash` (המודגש) גובר בעדיפות על `heroBreathe` כל עוד המחלקה `hero-flash` דבוקה לאלמנט — ולכן `flyCoinToHero()` **חייבת** להסיר את `hero-flash` בעצמה אחרי שהאנימציה שלה מסתיימת (`setTimeout(...,600)` אחרי ה-popEl), **בניגוד לקונבנציה הרגילה בקוד** של להשאיר מחלקות pop דבוקות לצמיתות (`record-glow`, `success-pop` וכו' נשארות — זה תקין שם כי אין שם אנימציית-רקע מתמשכת שהן חוסמות). **אם מוסיפים אנימציית-רקע תמידית לאלמנט אחר עם מחלקת popEl משלו, לזכור את אותה בעיה.**
8. **FAB "מזמין" בחוסר פעילות** — `lastActivityTs` + `setInterval` כל 15 שנ' בודק אם עברו 3 דקות בלי `pointerdown` בשום מקום; `pointerdown` כלשהו מנקה את `fab-invite` מיד. לא תלוי ב-`document.hidden` בלבד — גם נבדק שם.
9. **קונפטי זעיר בכל רבע-דרך ליעד** (25/50/75%) — `microConfetti()`, **לא** ה-`spawnCoinRain()`/`celebrate()` המלאים (זה נשאר למשיאים גדולים בלבד). מעקב אחרי אילו אחוזים כבר נראו הוא ב-**משתנה בזיכרון** `goalPctSeen` (לא ב-`state.meta`) — מתאפס בכל טעינת דף בכוונה, כדי לא להוסיף שדות state/סנכרון-ענן על משהו קוסמטי כל כך קטן.
10. **FAB "פועם" קרוב לשיא** — `closeToRecord = best>0 && todayInc>=best*0.85 && todayInc<best`, מחושב ומוצמד/מוסר בכל `renderHome()`.

**נבדק**: את כל 10 בנפרד (Playwright, כולל תיקון flakiness בבדיקות עצמן — לדוגמה בדיקת confetti/coin לפי `getComputedStyle(...).zIndex` ולא לפי חיפוש מחרוזת ב-`style` attribute, כי הדפדפן מנרמל את הפורמט של `cssText`), פלוס כל חבילות הרגרסיה הקיימות (רגילה, יומן, קולי, עדכון-ליבה) — אפס שגיאות.

## עדכון 2026-09-17: תיקון סנכרון אמיתי (union-merge), פייבוקס, עמלות עריכות, ימי עבודה, כביש 6 קטן (v15)

המשתמש חזר אחרי הפסקה עם רשימת תקלות ורעיונות, בהדגשה חוזרת ונשנית שבטיחות הנתונים היא **מעל הכל**: "הכי חשוב שלא יתאפס לאף אחד שום דבר". דיווח תרחיש קונקרטי: שני טלפונים על אותו קוד, טלפון ב' בלי אינטרנט זמן מה (זוכר רק את המצב האחרון שהיה לו), וברגע שהוא מתחבר מחדש ועושה רענון — הנתונים "מתאפסים ומתבלגנים".

### הבאג האמיתי: השוואת timestamp לא אמינה מול union-merge לפי ID
כל מנגנון ה-sync הקודם (כולל התיקונים הקודמים מ-2026-09-09, `pendingLocalPush`/`firstPushChecked`) עדיין התבסס בסופו של דבר על **השוואת `updatedAt`** בין local לremote כדי להחליט מי "מנצח". **זה שבור מיסודו**: `save()` תמיד חותם `state.updatedAt=Date.now()` **מיד**, כך שכל מכשיר — כולל אחד עם נתונים ישנים/חלקיים — מקבל timestamp "עכשווי" ברגע שהוא שומר, בלי קשר לכמה "אמיתי" ה-state שלו. מכשיר ב' שהיה אופליין וזוכר רק חלק מהתמונה, ברגע שהוא מתחבר ושומר כל דבר (אפילו טריוויאלי), ה-timestamp שלו "מנצח" ודורס נתונים אמיתיים ממכשיר א' שהיו כל הזמן בענן.

**נבדק ואומת בפועל**: נבנה טסט (`smoke_twophone.js`) שמשחזר בדיוק את התרחיש — טלפון א' מוסיף +500 ונשלח לענן, טלפון ב' (שהיה "אופליין", זוכר state ישן) מתחבר ושומר. עם ההשוואה הישנה: ה-500 נמחק מהענן. 

**התיקון**: הוחלפה **כל** לוגיקת ההשוואה לפי timestamp ב-**union-merge לפי ID** — `unionMergeState(base,extra)`: לכל אחד מ-`tx`/`shifts`/`journal`, פשוט מאחדים את שתי הרשימות לפי `id` (לא דורסים, רק מוסיפים את מה שחסר). מיושם בשני מקומות:
- `cloudPush()` — ה-push הראשון בכל טעינת עמוד (`firstPushChecked`) עושה `ref.get()` ואז `state=unionMergeState(state,remote)` **תמיד** (לא רק כש-state מקומי נראה ריק) — כלומר תמיד ממזגים עם הענן לפני שדוחפים, לא משנה מה מצב הנתונים המקומיים.
- `onSnapshot` (בתוך `startCloudSync()`) — כש-snapshot מגיע ממכשיר אחר: `state=unionMergeState(remote,state)` במקום `state=normState(remote)` (שהיה מחליף לגמרי).

**טרייד-אוף מכוון וברור שהוסבר ותועד**: union-merge **רק מוסיף, לעולם לא מוחק**. משמעות: תנועה שנמחקה במכשיר א' אבל עוד לא הספיקה להתפשט למכשיר ב' יכולה תיאורטית "לקום לתחייה" אם ב' עדיין זוכר אותה ומתמזג מאוחר יותר. **זו החלטה מכוונת** — אחרי הדגשים החוזרים ונשנים של המשתמש שאובדן הכנסה אמיתית הרבה יותר גרוע מהחייאה נדירה של פריט שנמחק, ואחרי תקרית איבוד-נתונים אמיתית שכבר קרתה לו בעבר (ר' עדכון 2026-09-09 למעלה). **אם רוצים אי פעם "מחיקה שמתפשטת נכון" — זה דורש tombstones (רשומת "נמחק ב-X" עם timestamp שנשמרת לצמיתות, לא רק הסרה מהרשימה) — לא בוצע כרגע כי מוסיף מורכבות משמעותית ולא התבקש.**
- **נבדקו מחדש כל טסטי הסנכרון הקיימים** (`smoke_syncrace`, `smoke_databackup`, `smoke_erasestillworks`, `smoke_syncnormal`) — כולם עדיין עוברים. שימו לב: `smoke_databackup.js` יש לו assertion ישן שאומר "should be 0 right after reload" שכבר לא מדויק (הריפוי העצמי מהיר יותר עכשיו הודות ל-`onSnapshot` merge) — **זה בסדר, התוצאה הסופית (שזה מה שחשוב) עדיין נכונה**, לא לתקן את ה-assertion הזה בלי להבין למה זה השתנה.

### הוסר: כרטיס "חדשות רכב ובנזין"
המשתמש ביקש להוריד לגמרי. הוסר כל ה-HTML/JS הקשור (`#carNewsCard`, `#fuelPriceVal`, `#trafficLinkBtn`, `#motLinkBtn` וכל ה-listeners שלהם) ממסך הדוחות.

### אשראי → פייבוקس (עם תאימות לאחור לנתונים ישנים)
`METHODS`/`ms`/`mKeys`/`mLbl`/`mCol` (בהיר וכהה) עודכנו: `paybox:'פייבוקס'` הוא עכשיו האופציה הפעילה בבחירת אמצעי תשלום (גם בכפתור ב-HTML, גם בהזרעת דמו). **`credit:'אשראי'` נשאר בכוונה** בכל האובייקטים האלה כ**fallback לתצוגה בלבד** — לא בחירה זמינה ב-UI, לא בפענוח קולי (`/אשראי/` הוחלף ב-`/פייבוקס/`) — אבל תנועות ישנות עם `method:'credit'` ימשיכו להציג "אשראי" נכון בכל מקום (רשימה/דוחות), **לא `undefined`**. זו הייתה החלטה יזומה שלי (לא התבקשה במפורש) כדי לשמור על "שום דבר לא נשבר" עבור נתונים היסטוריים — בדיוק ברוח דרישת המשתמש.

### הודעת כביש 6 — קומפקטית
`#roadAsk`/`.ra-card` עברו מ-modal גדול עם dimming לכרטיס-שורה קטן וצף (`.ra-row`: אייקון + כותרת/תת-כותרת, כפתורים `.btn.sm`). רוחב מקסימלי 300px, בלי backdrop dimming.

### עמלות סדרן — 4 כפתורים ניתנים לעריכה (כולל 0%)
הוחלף המנגנון הקבוע (30/25/12/10 + "אחר") ב-`state.settings.pctPresets` (מערך של 4 מספרים, ברירת מחדל `[10,12,25,30]`). `#pctChips` נרנדר דינמית מהמערך. **לחיצה רגילה** בוחרת פריסט; **לחיצה ארוכה (550ms, כמו ה-FAB)** פותחת `openSheet` לעריכת אותו סלוט — כולל **0% תקין** (`v=Math.max(0,Math.min(100,Math.round(v)))`, לא `v>0`). רמז טקסט קבוע מתחת לצ'יפים מסביר את הלחיצה הארוכה. **אין יותר "אחר"** — 4 הכפתורים עצמם ניתנים לעריכה מלאה, זה מחליף את הצורך בזה.
- **`settings.pct` (ברירת המחדל הכללית שמוצגת בהגדרות, ר' סעיף המודל העסקי למעלה) נשאר בדיוק כמו שהיה** — נפרד לגמרי מ-`pctPresets` (שהם רק קיצורי-דרך במסך ההוספה). **אל תבלבלי בין השניים.**

### ימי עבודה — הרצף לא נהרס בימים שאתה לא עובד
`state.settings.workDays7` (מערך 7 בוליאנים, א'-ש', ברירת מחדל כולם `true`) + `workDaysSet` (בוליאני, האם המשתמש כבר נשאל/אישר). `isWorkDay(iso)` בודקת יום נתון מול המערך; `currentStreak()`/`computeLongestStreak()` **עברו לדלג על ימים שאינם ימי-עבודה** בלי לשבור את הרצף (עם cap של 3650 איטרציות כרשת ביטחון).
- **מודל onboarding חד-פעמי** (`#workDaysModal`, `maybeShowWorkDays()`) לכל המשתמשים הקיימים — משורשר **אחרי** `maybeShowMonthCycle()` (וגם אחרי `applyNewMonthStart()`, למקרה שהמשתמש עדיין באמצע שרשרת המודלים): `closeWhatsNew()`→`maybeShowMonthCycle()`→(אם `monthCycleSet`)→`maybeShowWorkDays()`. **משתמש חדש** מקבל `workDaysSet=true` מיד ב-`cgGo`'s `'new'` branch — בדיוק כמו `lastSeenVer`/`monthCycleSet` — ולא רואה את זה לעולם.
- **ניתן לעריכה תמיד** דרך הגדרות — שורה "ימי העבודה שלך" (`#workDaysVal`, מציגה "כל השבוע" או "X ימים"). **תזכורת חשובה**: `refreshAll()` **לא** קוראת ל-`renderSettings()` — אם שומרים משהו שמשפיע על תצוגת ההגדרות (כמו כאן, `$('#wdSave')`), צריך לקרוא ל-`renderSettings()` **בנפרד**, לפני/אחרי `refreshAll()`, אחרת המסך נשאר עם ערך ישן עד ניווט הבא.

### אמינות רמקול/צליל
המשתמש דיווח "הרמקול לפעמים לא עובד, מחרפון". שני תיקונים אמיתיים (לא רק "זו מגבלת דפדפן"):
- **`AudioContext` משותף וקבוע** (`getAudioCtx()`) שנוצר פעם אחת ומופעל (primed) ב-`pointerdown` הראשון, במקום ליצור+לסגור context חדש בכל `playSaveSound()`/`playCelebrationSound()` — יצירה/סגירה חוזרת גורמת לפעמים ל-context להיתקע ב-`suspended` בכרום (באג ידוע). גם הוסר `setTimeout(...ctx.close()...)` שהיה שריד מיותר.
- **`speak()`**: נוסף `synth.resume()` לפני כל קריאה (ספארי/כרום לפעמים משאירים את ה-`speechSynthesis` תקוע), ותמיכה ב-`opts.notifyFail` — קריאות **יזומות** (כפתור השמעת יום, כפתור יומן) מציגות טוסט כישלון אם `onerror`/timeout, כדי שהמשתמש ידע שזה לא עבד במקום לחשוב שהאפליקציה תקועה. קריאות **אוטומטיות** (ברכת בוקר) נשארות שקטות בכישלון — בכוונה, לא רוצים טוסט לא-מוסבר על משהו שהמשתמש לא ביקש באופן ישיר.

### תיקון דיוק: כיתוב ההגדרות "ללא שרת" היה שגוי
נמצא בביקורת ויזואלית עצמאית (לא התבקש במפורש) — הכיתוב התחתון בהגדרות אמר "כל הנתונים נשמרים רק במכשיר שלך · ללא שרת, ללא הרשמה", אבל האפליקציה **מחייבת** קוד סנכרון וענן Firebase לכל משתמש (`#codeGate` חוסם כניסה בלי `syncCode`) כבר מאז שפיצ'ר הסנכרון נוסף. תוקן לכיתוב מדויק: "מגובים אוטומטית לענן לפי הקוד האישי שלך · ללא הרשמה, ללא סיסמה" — עדיין מדגיש את מה שנכון (אין הרשמה עם סיסמה/אימייל) בלי לטעון טענת פרטיות שקרית.

### ביקורת ויזואלית מלאה
בוצע audit מסך-אחר-מסך (Playwright + צילומי מסך, כולל full-page) על בית/הוספת הכנסה/הוצאה/יומן/דוחות (3 עמדות גלילה)/הגדרות (מלא) — כל הכפתורים/תוויות/עריכה במקום, שום חפיפה, שום דבר חתוך. הפריט היחיד שנמצא ותוקן הוא כיתוב ה"ללא שרת" למעלה.

**נבדק**: כל 20+ חבילות smoke הקיימות הורצו מחדש (כולל `smoke_twophone.js` הייעודי לבאג הזה) — אפס שגיאות. שתי "כשלים" זמניים ב-`smoke_animations.js` (confetti/fab-heartbeat) התבררו כתלות-סדר בין הצעדים בתוך אותו קובץ טסט עצמו (state מצטבר דוחף את `meta.best`/סף היעד מעבר למה שהטסט הישן הניח) — **אומת ישירות** מול `smoke_confetti_heartbeat.js` (טסט מבודד ונקי לאותה לוגיקה בדיוק) שעבר נקי; לא נגעתי בלוגיקת confetti/heartbeat עצמה כי היא לא הייתה שבורה.

**גרסה: APP_VER=15, `sw.js` CACHE=kupa-v3-22.**

## עדכון 2026-09-17 (המשך אותו יום): מצב "רכב מכוסה" — סיפור דו-שלבי לחודש (v16)

עוד באותה שיחה, המשתמש ביקש רעיון נוסף: "כל יום זה מוריד 250 עד סוף חודש... נגיד אני עשיתי כבר 6000, אני רוצה לשים את זה בצד ולהתחיל כאילו מהתחלה בלי ה-250 האלה... זה נותן לי יותר סיפור... אני רוצה שכל נסיעה תיתן מוטיבציה." הכוונה: ברגע שהרווח החודשי מכסה את עלות הרכב הקבועה, "היום שלך" יפסיק "להעניש" כל יום בעלות הרכב היומית המוחלקת (250 בדוגמה שלו) — כאילו שלב א' (לכסות את הרכב) הסתיים ושלב ב' (רווח טהור) מתחיל.

### חשוב: זה לא נגע בשום מקום בחשבונאות האמיתית
**הנטו החודשי האמיתי (`#netNum`, דוחות, CSV, יעד) לא השתנה בשום צורה ולא תלוי במתג הזה בכלל** — הפיצ'ר משפיע **רק** על תצוגת כרטיס "היום שלך" (המספר המוחלק היומי), שממילא הוא כלי-הצגה בלבד ולא חלק מהחשבונאות החודשית (ר' `carDailyCost()` בסעיף המקורי — הוא כבר לא נכנס ל-`net` החודשי, רק ל-`bal` היומי). זו הייתה נקודה קריטית לוודא לפני שבונים משהו שנוגע ב"כסף" — **נבדק במפורש** (`smoke_breakeven.js`): הנטו החודשי האמיתי זהה לפני ואחרי הפעלה/כיבוי של המתג, בכל שלב.

### הבעיה שנתקלתי בה בדרך: איזה "רווח" בדיוק צריך להשוות מול העלות הקבועה
ניסיון ראשון: להשוות את ה-`net` החודשי המלא (`netOf(sums(monthTx),pct)`) מול `rent+insuranceMonthly` — **התברר כשגוי מתמטית**. הסיבה: `ensureRentExpense()`/`ensureInsuranceExpense()` כבר מוסיפות תנועת הוצאה **אמיתית** (lump sum מלא, פעם בחודש) שנכנסת ל-`net` החודשי האמיתי — כלומר `net` כבר "ספג" את עלות הרכב הקבועה פעם אחת. להשוות אותו שוב מול אותה עלות קבועה אומר "תרוויח את שכר הדירה **פעמיים**" לפני שהמתג ידלוק (נבדק ישירות בטסט וראינו net שלילי במקום שהיה אמור כבר להיות מכוסה). **הפתרון**: `operatingProfit(y,m)` — פונקציה נפרדת שמחשבת רק הכנסה פחות עמלת סדרן פחות **הוצאות משתנות** (הכל חוץ מ-`rent`/`insurance`) — כלומר "כמה הרווחתי מנסיעות בפועל, בלי להביא בחשבון את עלות הרכב הקבועה בכלל." ברגע שזה עובר את `fixedMonthlyCost()` (=`rent+insuranceMonthly`, אותה פונקציה ש-`carDailyCost()` כבר משתמשת בה), נחשב "מכוסה". **אם משנים אי פעם את ההגדרה הזו, לזכור את המלכודת הזו — אל תשווי מול `net`/`netOf` הרגילים, הם כבר כוללים את ההוצאה הקבועה בפועל.**

### מנגנון
- `state.settings.breakEvenOn` (ברירת מחדל **true** — זה בדיוק מה שהמשתמש ביקש לחוות מיד, לא פיצ'ר-אופציונלי-כבוי-כברירת-מחדל כמו אחרים).
- `state.meta.breakEvenCycle` — שומר את מפתח תחילת המחזור (`cycleStartISO`) שבו כבר נחגג "כיסוי" הפעם — כך שהחגיגה הגדולה קורית **פעם אחת בלבד לכל מחזור** (מתאפס אוטומטית במחזור הבא כי המפתח משתנה).
- `renderHome()`: כש-`monthlyCarCovered(ui.hm.y,ui.hm.m)` אמת — `#tCar` מציג "✓ מכוסה" במקום "−250", `bal` מחושב בלי `car` בכלל, פס ההתקדמות קופץ ל-100%, וההודעה התחתונה משתנה ל-"הרכב מכוסה החודש 🎉 מכאן והלאה — הכל רווח נקי".
- `saveTx()`: ברגע החצייה בפועל (`justCovered`) — עדיפות חגיגה **אחרי יעד חודשי, לפני אבני-דרך/שיא יומי** (ר' סדר העדיפויות המקורי בסעיף האנימציות): `celebrate('🚗💰','כיסית את הרכב לחודש!',...)`. בכל נסיעה **נוספת** באותו מחזור אחרי שכבר מכוסה (`alreadyCovered`) — לא חוגגים שוב, אלא מציגים תג צף קטן וטעים ליד המספר הגדול: `showPureProfitBadge()` ("💰 100% רווח", פיל ירוק שדוהה-פנימה-ודוהה-החוצה, בערך 1.5 שנ' סה"כ, כמו שאר האנימציות הצפות בקוד — לא CSS class ייעודי, DOM+inline-style ישירות, אותו סגנון בדיוק כמו `flyCoinToHero`/`microConfetti`).
- הגדרות: שורה חדשה בכרטיס "רכב וסדרן" (`#breakEvenRow`, אחרי שורת "עמלה לסדרן — ברירת מחדל") — מתג + טקסט הסבר, **לא** נראה כמו checkbox יבש כי הוא בדיוק באותה שורת-הגדרה (label+sub+switch) שכל שאר המתגים היפים בהגדרות כבר משתמשים בה (autoRent/autoIns/carModeOn וכו') — זו הייתה בקשה מפורשת של המשתמש ("שההגדרה תראה אחד היפים ולא משהו פשוט"), ונפתרה פשוט על ידי שימוש עקבי ברכיב הקיים במקום להמציא UI חדש.

### מלכודת טסט שנתקלתי בה (לא קשורה לבאג אמיתי, לתיעוד עתידי)
טסט ראשוני שבדק "אין חגיגה חוזרת בנסיעה השנייה" נכשל — אבל לא בגלל הפיצ'ר: קפיצה חד-פעמית גדולה בהכנסה (למשל 6100 ש"ח לקיוח) חוצה בבת אחת כמה ספי-אבן-דרך (`checkMilestones`, THRESH=[...,5000,1000]) — `checkMilestones()` מחזירה ומסמנת **רק את הסף הראשון** שנתקל בו בלולאה, כך שסף קטן יותר (1000) שגם נחצה **נשאר "ממתין"** ומופיע בקריאה הבאה ל-`checkMilestones` (השמירה הבאה), גם אם היא לא קשורה בכלל לרכב-מכוסה. זו התנהגות קיימת ומכוונת של מנגנון אבני-הדרך (לא משהו שהוספתי/שברתי) — רק צריך לזכור אותה בבניית טסטים עתידיים סביב שמירות גדולות-בבת-אחת: לסמן מראש (`state.meta.milestones['life_'+th]=true`) את כל הספים הרלוונטיים כדי לבודד את מה שבאמת בודקים.

**נבדק**: `smoke_breakeven.js` (המסלול המלא: לפני כיסוי → כיסוי חלקי → חצייה עם חגיגה אמיתית → מכוסה עם תג רווח → כיבוי המתג חוזר להתנהגות הישנה, וכל הזמן הנטו החודשי האמיתי משתקף נכון ולא מושפע מהמתג) + `smoke_breakeven_badge_debug.js` (מוודא שהתג הצף `showPureProfitBadge()` באמת נוצר ב-DOM עם ה-DOM position/opacity הנכונים) + כל חבילות הרגרסיה הקיימות הורצו מחדש בגלל שהשינוי נגע בפונקציות מרכזיות (`saveTx`,`renderHome`,`carDailyCost`) — אפס שגיאות.

**גרסה: APP_VER=16, `sw.js` CACHE=kupa-v3-23.**

## עדכון 2026-09-17 (המשך): "רכב מכוסה" צריך מחזור-חיוב-רכב, לא מחזור-דוחות (תיקון אמיתי) + באג הקראה + התחלת אשף פרימיום (v17)

המשתמש דיווח, אחרי שפרסתי את "רכב מכוסה" ל-production: "אני פותח את האפליקציה זה עדיין מראה לי את ה-250". גם: "ואחר כך נגיד סוף חודש שהוא הגדיר ממתי שהוא מביא את הרכב והוא רושם את התאריך אתה מחשב את זה חודש לא מהרישון לראשון" — כלומר: ה"חודש" של הרכב צריך להיות לפי **תאריך החיוב האמיתי של הרכב** (`billingDay`, שכבר קיים!), לא לפי מחזור-הדוחות הכללי (`monthStartDay`/`cycleOf`) שבו השתמשתי בטעות בבנייה המקורית.

### הבאג האמיתי שנמצא
`operatingProfit`/`monthlyCarCovered` השתמשו ב-`monthTx(y,m)` → `cycleStartISO`/`cycleEndISO` → מבוססי `state.settings.monthStartDay`. אבל התשלום האמיתי על הרכב (`ensureRentExpense`) קורה לפי `billingDay` על **חודש קלנדרי אמיתי**, לא לפי מחזור-הדוחות. אלה **שני מחזורים שונים לגמרי** שכבר תועדו כנפרדים בסעיף "מחזור חודשי אישי" למעלה (`ensureRentExpense`/`ensureInsuranceExpense` "לא קשורות למחזור הזה בכלל"). כשהמשתמש שינה/הגדיר `monthStartDay` שונה מ-`billingDay`, "החודש" של הרכב-מכוסה לא תאם בכלל למחזור שבו באמת יורד לו התשלום — התוצאה: הפיצ'ר יכל "לפספס" חצייה אמיתית או לחשוב שיש חצייה כשלא הייתה.

**התיקון**: נוספו `carCycleStartDate/carCycleStartISO/carCycleEndISO/carCycleOf/carCycleTx` — בדיוק אותו מבנה כמו `cycleStartDate/.../cycleOf/monthTx` הקיימים, אבל עם `state.settings.billingDay` במקום `state.settings.monthStartDay`. `operatingProfit(y,m)` עברה מ-`monthTx` ל-`carCycleTx`. **קריטי**: ה-`(y,m)` שמועבר ל-`monthlyCarCovered`/`operatingProfit` הוא עכשיו אינדקס **מחזור-רכב**, לא מחזור-דוחות — אסור להעביר לזה `ui.hm` (זה מחזור-דוחות!). `renderHome()` עכשיו מחשבת `carCycleOf(td)` (td = היום שבו צופים, `ui.day`) לפני קריאה ל-`monthlyCarCovered`. `saveTx()` משתמשת ב-`carCycleOf(d)` (משתנה נפרד, `carCycD`, לא `cycD` — `cycD`/`cycleOf(d)` נשארים בשימוש **בדיוק כמו קודם** לעדכון `ui.hm`/`ui.day`, שום דבר שם לא השתנה) והמפתח לשער `breakEvenCycle` הוא `carCycleStartISO`, לא `cycleStartISO`. **נבדק** (`smoke_breakeven_billingday.js`): עם `billingDay=20`, הכנסה גדולה (9000) שנרשמה **לפני** ה-20 (במחזור-רכב הקודם) בכוונה **לא** מכסה את המחזור הנוכחי — רק הכנסה בתוך מחזור-הרכב הנוכחי סופרת.

### באג אמיתי נוסף: `speak()` היה נעול מאחורי מתג לא-קשור
תלונה: "ההקראה של כמה עשיתי היום לא עובד". נמצא: `speak()` בדק `if(!state.settings.soundOn)return` **לפני הכל** — אבל `soundOn` (ברירת מחדל **false**) נועד במקור לצליל-קליק-קטן-בשמירה (Web Audio, `playSaveSound`), **לא** לקריאת-טקסט-בקול. כתוצאה: כל משתמש שלא הדליק במפורש "צליל" בהגדרות (רוב המשתמשים, כי ברירת המחדל כבויה) — לחיצה על כפתור הרמקול המפורש ("השמעת היום"/"השמעת יום" ביומן) לא הייתה עושה כלום מלבד טוסט לא-בולט. **התיקון**: `speak()` עכשיו בודקת `if(!opts.notifyFail&&!state.settings.soundOn)return` — כלומר **לחיצה מפורשת על כפתור השמעה** (מזוהה לפי `opts.notifyFail:true`, שכבר היה הסימון הקיים ל"קריאה יזומה") **תמיד** מנסה להשמיע, בלי קשר למתג "צליל" הכללי; רק קריאות **אוטומטיות/לא-מבוקשות** (ברכת בוקר, שנקראת בלי `notifyFail`) עדיין מכבדות את `soundOn`. **ההיגיון**: לחיצה מפורשת על 🔊 היא כבר "אישור" חד-משמעי לשמוע — אין סיבה לדרוש גם מתג נפרד דלוק. **אל תחזירי את זה למתג אחיד** בלי לזכור שזה ינעל שוב את הכפתורים המפורשים למי שלא הדליק "צליל" (רוב המשתמשים).

### אשף onboarding פרימיום — שם, ברכה אישית, הסבר קצר (v18)
המשתמש ביקש: "כניסה יפה... שאלות שירגיש שזה באמת רמה גבוהה... מה השם שלך... שלום נהוראי... מסביר טיפה על האפליקציה בקטנה בלי לחפור... ואז כל השאלות לגבי רכב." נוספו **שני מסכים חדשים לפני** שאלת "איך הרכב אצלך" הקיימת (`wizStep1`), רק בהרצה הראשונה (לא כשמריצים "עדכון פרופיל" מהגדרות):
- `wizWelcome()` — 👋 + `<input>` טקסט חופשי לשם (`#wizNameInput`/`#wizNameGo`, גם Enter עובד). **השם נשמר ל-`state.settings.name` מיד**, לא מחכה ל-`wizFinish()` — כך שאם המשתמש נוטש באמצע, השם כבר לא הולך לאיבוד.
- `wizIntro(name)` — "שלום [שם] 👋" + 3 שורות תמצית קצרות (עמלת סדרן+רכב מחושבים אוטומטית / גיבוי ענן אוטומטי / "עכשיו כמה שאלות על הרכב") לפני מעבר ל-`wizStep1()` הקיים.
- **`openWizard(isFirstRun)`** — פרמטר חדש (בעבר בלי פרמטרים): `true` רק מ-`finishCodeGate()` (הרצה ראשונה אמיתית); כפתור "עדכון פרופיל" בהגדרות ממשיך לקרוא בלי פרמטר → מדלג ישר ל-`wizStep1` בדיוק כמו קודם (משתמש קיים כבר יש לו שם, אין טעם להציג שוב).
- **`wizProgress()`** — נקודות התקדמות (`.wiz-dot`, 6 בסה"כ) בראש כל מסך wizShow-מבוסס (welcome/intro/ownership/insurance) — **לא** במסכי `openSheet`/`openDayPicker` (רכיבים נפרדים, לא תומכים בזה) ולא במסך הסיום (חגיגה, לא "עוד שלב"). מתקדם ב-1 בכל קריאה — **אם מוסיפים מסך wizShow חדש לרצף, קראו ל-`wizProgress()` בתוך ה-HTML שלו כדי שהנקודות ימשיכו נכון.**
- מסך הסיום (`wizFinish`) עודכן לברך בשם ("מוכן, [שם]!") עם אימוג'י 🎉 וכפתור סיום יותר אנרגטי ("קדימה, בואו נרוויח").
- **קריטי לבדיקות**: זה משנה את רצף ה-onboarding שכל 30+ קבצי smoke test מסתמכים עליו (כולם עושים `#cgGo` ואז ישר `[data-wiz="owned/rent/..."]`). **כל קובץ טסט קיים עודכן** להוסיף לפני כן: `#wizNameInput` fill + `#wizNameGo` click + `#wizIntroGo` click. **שימו לב**: כמה קבצים (`smoke_journal.js`, `smoke_confetti_heartbeat.js`) מריצים את רצף ה-onboarding **פעמיים** בתוך אותו קובץ (חשבון שני/עמוד שני) — צריך לתקן את **שני** המופעים, לא רק הראשון; `smoke_twophone.js` השתמש ב-`page1`/`page2` (לא `page`) — לשים לב לשם המשתנה הנכון בכל קובץ בעתיד.

**הערה על גרסה**: השינוי הזה **בלתי-נראה למשתמשים קיימים לגמרי** — `openWizard(true)` נקרא רק מ-`finishCodeGate()` כש-`!wizardDone`, וכל משתמש קיים כבר `wizardDone=true`. לכן **לא הועלה `APP_VER`/לא נגע `WHATS_NEW`** (אין מה "להכריז" על שינוי שהם לעולם לא יראו) — רק `sw.js` CACHE עלה כדי שהקוד יהיה מוכן לכל הרשמה חדשה הבאה.

### נעילה ביומטרית — טביעת אצבע / Face ID דרך WebAuthn (v19)
המשתמש ביקש נעילה ביומטרית אמיתית (סמסונג/אנדרואיד = טביעת אצבע, אייפון = Face ID) בנוסף לקוד 4 הספרות הקיים. נבנה עם **WebAuthn** (`navigator.credentials.create`/`get`, `authenticatorAttachment:'platform'`) — ה-API האמיתי היחיד לביומטריה בדפדפן/PWA. **אין מצב "מזויף"**: אם הדפדפן/מכשיר לא תומך ב-platform authenticator, האופציה פשוט לא מוצגת (בדיוק כמו `voiceBtn` שמוסתר בלי תמיכה קולית).

- **מקומי למכשיר בלבד, לא מסונכרן**: מזהה הקרדנציאל (`localStorage['kupaNehed.biocred']`) נשמר **מחוץ ל-`state`** לגמרי — לא ב-`settings`, לא ב-`meta`, לא נדחף לענן. הסיבה: קרדנציאל WebAuthn שנרשם בטלפון א' **לא תקף** בטלפון ב' (כל מכשיר עם חיישן ביומטרי משלו) — לו זה היה חלק מ-`state` המסונכרן, טלפון שני "יחשוב" שיש לו ביומטריה מופעלת ויכשל כל פעם. **קוד ה-PIN (`state.meta.lockPin`) נשאר הגיבוי היחיד שמסונכרן בין מכשירים.**
- `bioAvailable()` — בודקת `PublicKeyCredential.isUserVerifyingPlatformAuthenticatorAvailable()` (אסינכרוני) — שורת הגדרות `#bioRow` מוצגת רק אם זה `true` **וגם** הנעילה הראשית דלוקה (ביומטריה היא תוספת לקוד, לא תחליף לו).
- `registerBiometric()`/`tryBiometricUnlock(silent)` — הרשמה/אימות דרך `navigator.credentials.create/get`. ב-`showLockGate('unlock')`, אם יש קרדנציאל שמור: מוצג כפתור ביומטרי ייעודי (טקסט/אייקון לפי פלטפורמה — `bioLabel()` מזהה אייפון/מק ומציג "Face ID / Touch ID", אחרת "טביעת אצבע") **וגם** מתבצע ניסיון אוטומטי (`silent:true`, אחרי 350ms) — כך שברוב המקרים המשתמש בכלל לא צריך ללחוץ כלום, אבל אם הדפדפן חוסם פרומפט אוטומטי בלי ג'סטורה, הכפתור הידני עדיין שם. **קוד ה-PIN תמיד נשאר גלוי מתחת כגיבוי** — אף פעם לא חוסמים גישה רק לביומטריה.
- **כיבוי הנעילה הראשית מנקה גם את הקרדנציאל הביומטרי** (`clearBioCred()` ב-`#lockSwitch` change-handler) — כדי לא להשאיר קרדנציאל "יתום" רשום בלי נעילה בכלל.
- `unlockSuccess()` — פונקציה משותפת חדשה (היה קוד כפול בין הצלחת PIN לביומטריה) שמסמנת `sessionStorage.kupaUnlocked` וסוגרת את השער.

**מגבלת בדיקה אמיתית (כמו הקולי)**: ניסיתי לבנות טסט Playwright עם virtual authenticator של Chrome DevTools Protocol (`WebAuthn.addVirtualAuthenticator`) כדי לדמות חיישן טביעת אצבע אמיתי — ה-CDP domain עצמו לא הסכים לאשר את ה-ceremony בסביבה הזו (`NotAllowedError` עקבי, גם עם הרשאות UI מבוטלות, גם לא-headless, גם עם/בלי `userVerification`) — כנראה מגבלת סביבת ה-sandbox/headless שלא קשורה לקוד עצמו. **נבדק ידנית מה שכן ניתן לבדיקה אוטומטית**: שהקוד נטען בלי שגיאות JS, שרצף ה-PIN הקיים (`smoke_stage2.js`) ממשיך לעבוד בדיוק כמו קודם ללא שום נסיגה, ושכל שאר הרגרסיה לא נפגעה. **הבדיקה האמיתית של טביעת אצבע/Face ID בפועל דורשת מכשיר אמיתי** — בדיוק כמו זיהוי קולי אמיתי שתועד למעלה, זו מגבלת סביבת הפיתוח, לא משהו שדילגתי עליו בכוונה.

**גרסה: APP_VER=18, `sw.js` CACHE=kupa-v3-26** (בשונה מהאשף — זה כן נראה לכל מי שכבר מפעיל נעילה, אז מגיע לו עדכון WHATS_NEW).
